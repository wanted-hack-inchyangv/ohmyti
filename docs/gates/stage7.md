# 7단계 게이트: 페르소나 4종의 키트·리포트 회귀 (T-708)

`pnpm gate:personas`가 만든 기록이다. 배포 환경에 페르소나 4종을 제출해 실제 LLM으로 인터뷰 키트(T-702·T-704)와 채용 리포트(T-705·T-706)를 만들고, 키트의 질문 슬롯·우선순위·유형별 질문 수를 `samples/personas/expected-matrix.json`의 기대값과 대조한다. 질문은 모두 결정적 검사(`lintInterviewQuestion`)를 통과하고 저장된 근거를 하나 이상 참조해야 하며, 리포트의 판정·점수는 워크벤치(평가 조회 API)의 값과 같아야 하고 시스템이 쓴 문장에 금지 표현(PRD 14.4)이 없어야 한다. 기본 질문 대체와 버린 LLM 출력은 품질 관찰이라 경고로 남긴다.

- 결과: **통과**
- 배포: https://ohmyti.vercel.app
- 실행 일시: 2026-09-20T01:36:50.996Z ~ 2026-09-20T01:49:01.238Z
- rubric: `v2-be07fb44`

## 키트 생성 요약

| 페르소나 | 평가                                                                                     | 질문 수 | 기본 질문 대체 | LLM | 모델 · 프롬프트                            | 버린 항목                                                                                                                                              | 진행안(45·60분)               |
| -------- | ---------------------------------------------------------------------------------------- | ------: | -------------: | --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------- |
| seojin   | [`0f3b8cbe`](https://ohmyti.vercel.app/evaluations/0f3b8cbe-c5a1-4f64-a9c5-6a1ccee20fa0) |       9 |            1/9 | OK  | deepseek-flash · interview-kit@v1+9129ad0e | 없음                                                                                                                                                   | 45분 45분·6문 / 60분 45분·6문 |
| taeyun   | [`65711c8c`](https://ohmyti.vercel.app/evaluations/65711c8c-7660-4c69-9517-fb80911f499a) |      11 |           5/11 | OK  | deepseek-flash · interview-kit@v1+9129ad0e | STRENGTH_DEPTH:R-03 LINT_VIOLATION, STRENGTH_DEPTH:R-04 LINT_VIOLATION, DESIGN_TRADEOFF:R-12 LINT_VIOLATION, EXTENSION:persistent-store LINT_VIOLATION | 45분 44분·5문 / 60분 55분·7문 |
| gaeun    | [`069a839f`](https://ohmyti.vercel.app/evaluations/069a839f-ad1b-43a0-8d98-80adffad73a6) |      14 |           3/14 | OK  | deepseek-flash · interview-kit@v1+9129ad0e | EXTENSION:multi-instance LINT_VIOLATION, EXTENSION:persistent-store LINT_VIOLATION                                                                     | 45분 44분·5문 / 60분 58분·7문 |
| dohyun   | [`8b759d05`](https://ohmyti.vercel.app/evaluations/8b759d05-c1fb-4637-9fab-3786bbf35162) |      11 |           2/11 | OK  | deepseek-flash · interview-kit@v1+9129ad0e | EXTENSION:persistent-store LINT_VIOLATION                                                                                                              | 45분 45분·6문 / 60분 51분·7문 |

## 채용 리포트

| 페르소나 | 점수 표시(워크벤치 = 리포트) | 판정 분포                       | 필수 질문 | 이력서 연결   | 설계 검토 | PDF 쪽 수 (리포트 / 키트) | 금지 표현 |
| -------- | ---------------------------- | ------------------------------- | --------: | ------------- | --------- | ------------------------- | --------- |
| seojin   | 90~100/100 · 10점 검토 대기  | PASS 14, INCONCLUSIVE 1         |         3 | AVAILABLE 4건 | AVAILABLE | 6 / 8                     | 0건       |
| taeyun   | 78~88/100 · 10점 검토 대기   | PASS 12, FAIL 2, INCONCLUSIVE 1 |         3 | AVAILABLE 5건 | AVAILABLE | 6 / 8                     | 0건       |
| gaeun    | 37~57/100 · 20점 검토 대기   | PASS 6, FAIL 6, INCONCLUSIVE 3  |         4 | AVAILABLE 5건 | AVAILABLE | 8 / 8                     | 0건       |
| dohyun   | 75~85/100 · 10점 검토 대기   | PASS 11, FAIL 3, INCONCLUSIVE 1 |         3 | AVAILABLE 4건 | AVAILABLE | 7 / 8                     | 0건       |

### 화면 캡처 (채용 리포트 · 인터뷰 키트)

| 화면                 | 뷰포트  | HTTP | 가로 넘침 | 콘솔 오류 | 파일                                                 | 문제 |
| -------------------- | ------- | ---: | --------: | --------: | ---------------------------------------------------- | ---- |
| report-seojin        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-seojin-desktop.png`        | 없음 |
| interview-kit-seojin | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-seojin-desktop.png` | 없음 |
| report-seojin        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-seojin-mobile.png`         | 없음 |
| interview-kit-seojin | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-seojin-mobile.png`  | 없음 |
| report-taeyun        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-taeyun-desktop.png`        | 없음 |
| interview-kit-taeyun | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-taeyun-desktop.png` | 없음 |
| report-taeyun        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-taeyun-mobile.png`         | 없음 |
| interview-kit-taeyun | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-taeyun-mobile.png`  | 없음 |
| report-gaeun         | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-gaeun-desktop.png`         | 없음 |
| interview-kit-gaeun  | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-gaeun-desktop.png`  | 없음 |
| report-gaeun         | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-gaeun-mobile.png`          | 없음 |
| interview-kit-gaeun  | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-gaeun-mobile.png`   | 없음 |
| report-dohyun        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-dohyun-desktop.png`        | 없음 |
| interview-kit-dohyun | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-dohyun-desktop.png` | 없음 |
| report-dohyun        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-dohyun-mobile.png`         | 없음 |
| interview-kit-dohyun | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-dohyun-mobile.png`  | 없음 |

## 페르소나별 키트 전문

### seojin · seojin-order-api

#### Q1. 재고 부족으로 주문이 거절되는 경로를 구현하셨는데, 재고 확인과 차감이 어느 시점에 어떤 순서로 일어나는지 설명해 주시겠어요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 재고 부족 거절이 성립하는 조건과 재고가 변하지 않게 하는 처리 순서를 지원자가 설명할 수 있는지 확인한다.
- 꼬리 질문: 재고와 같은 수량의 주문이 통과하려면 비교 조건을 어떻게 두셨나요? / 재고 확인과 차감 사이에 다른 요청이 끼어들면 어떤 일이 생기나요? / 거절 응답을 만들 때 재고를 먼저 건드리지 않도록 어떤 순서를 택하셨나요?
- 좋은 답변의 신호: 재고 비교와 차감의 순서를 코드 흐름으로 구체적으로 설명한다. / 거절 시 재고가 그대로 남는 이유를 상태 변화 관점에서 말한다. / 재고와 같은 수량이 통과하는 경계 조건을 명시적으로 언급한다.
- 우려 신호: 재고 확인과 차감의 순서를 구분하지 않고 뭉뚱그려 말한다. / 거절 경로에서 재고가 변하지 않는 이유를 설명하지 못한다.
- 근거: 기준 R-03, 실행 기록 a1dfd914 · R-03, 코드 src/http/routes.ts:46-67, 코드 src/http/routes.ts:40-44

#### Q2. 주문 생성 요청의 입력 검증을 구현하셨는데, 어떤 입력들을 어떤 기준으로 걸러내는지 설명해 주시겠어요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 검증 대상과 판정 기준을 지원자가 스스로 열거하고, 검증 실패가 이후 상태에 영향을 주지 않는지 확인한다.
- 꼬리 질문: 수량의 타입과 범위를 판정할 때 어떤 조건을 쓰셨나요? / 헤더가 비어 있는 경우와 없는 경우를 어떻게 구분하셨나요? / 검증 실패한 요청의 키가 이후 재사용될 때 어떤 일이 일어나나요?
- 좋은 답변의 신호: 검증 항목을 타입·범위·필수 여부로 나누어 설명한다. / 검증 실패 시 상태가 변하지 않는다는 점을 근거와 함께 말한다. / 실패한 요청이 멱등성 기록에 남지 않는 흐름을 설명한다.
- 우려 신호: 검증 항목을 일부만 언급하고 나머지는 확인하지 않는다. / 검증 실패 요청이 이후 같은 키 처리에 미치는 영향을 말하지 못한다.
- 근거: 기준 R-04, 실행 기록 e8cbec12 · R-04, 코드 src/http/routes.ts:46-67, 코드 src/http/routes.ts:40-44

#### Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어오는 상황을 가정해 보겠습니다. 지금 구조에서 어떤 지점이 가장 먼저 문제가 될지 어떻게 판단하시겠어요? <sub>요구사항 확장 · MUST · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인메모리 상태를 가진 구조에서 다중 인스턴스로 확장할 때의 병목 지점과 판단 근거를 확인한다.
- 꼬리 질문: 인스턴스마다 상태가 따로 존재할 때 멱등성 보장은 어떻게 달라지나요? / 재고 차감의 원자성을 인스턴스 간에 유지하려면 무엇이 필요하다고 보시나요? / 지금 구조를 유지하면서 확장하는 방식과 저장소를 분리하는 방식의 비용 차이는 무엇인가요?
- 좋은 답변의 신호: 공유 상태의 위치를 기준으로 문제 지점을 특정한다. / 멱등성과 재고 차감을 각각 다른 문제로 구분해 설명한다. / 확장 방식의 비용과 제약을 함께 언급한다.
- 우려 신호: 다중 인스턴스에서 상태 불일치 가능성을 인식하지 못한다. / 확장을 언급하면서 기존 보장이 어떻게 유지되는지 설명하지 않는다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q4. 라우트 등록 함수가 가장 긴 함수로 관측되었는데, 이 함수가 담당하는 일을 어떻게 나누어 보고 계신지 설명해 주시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 가장 긴 함수의 책임 범위와 분리 기준을 지원자가 어떻게 인식하는지 확인한다.
- 꼬리 질문: 이 함수 안에서 요청 파싱과 도메인 규칙이 어디에서 갈라지나요? / 함수를 나눈다면 어떤 기준으로 경계를 정하시겠어요? / 지금 길이를 유지할 때와 나눌 때 각각 어떤 비용이 있다고 보시나요?
- 좋은 답변의 신호: 함수 내부의 책임을 종류별로 구분해 설명한다. / 분리 기준을 변경 빈도나 의존 방향 같은 근거로 제시한다. / 유지와 분리의 비용을 함께 비교한다.
- 우려 신호: 함수의 책임을 구분하지 않고 하나로 설명한다. / 분리 기준을 근거 없이 일반론으로만 말한다.
- 근거: 코드 src/http/routes.ts:30-80, 기준 R-12

#### Q5. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 저장 구조에서 무엇을 바꿔야 한다고 보시는지 설명해 주시겠어요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 인메모리 저장을 영속 저장으로 바꿀 때의 변경 범위와 초기화 경로 처리 방식을 확인한다.
- 꼬리 질문: 저장소를 바꿀 때 주문 생성 로직에서 수정이 필요한 부분은 어디인가요? / 재시작 후 멱등성 기록은 어떻게 다루어야 한다고 보시나요? / 상태 초기화 경로는 영속 저장에서 어떻게 동작해야 하나요?
- 좋은 답변의 신호: 저장소 교체 시 영향받는 모듈 범위를 구체적으로 짚는다. / 멱등성 기록의 영속 필요성을 별도로 언급한다. / 초기화 경로와 영속 저장의 관계를 설명한다.
- 우려 신호: 저장소 교체가 주문 생성 로직 전체 수정을 요구한다고만 말한다. / 재시작 후 멱등성 기록 처리에 대해 언급하지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q6. 상품 단위 재고 선점 시스템에서 재고 차감을 직렬화할 때 어떤 지점에서 충돌을 감지하고 재시도하도록 설계하셨나요? <sub>이력서 연결 · SHOULD · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적은 재고 선점 설계의 충돌 감지·재시도 구조를 본인이 설명할 수 있는지 확인한다.
- 꼬리 질문: TTL 기반 예약과 버전 비교 기반 낙관적 잠금을 각각 어느 조건에서 선택했나요? / 동시에 같은 상품을 선점하려는 요청이 겹칠 때 어느 단계에서 하나만 통과시키나요? / 재시도 횟수와 백오프를 늘리면 정합성과 지연이 각각 어떻게 달라지나요?
- 좋은 답변의 신호: 충돌 감지 지점과 재시도 조건을 코드 수준에서 구분해 설명한다 / TTL 만료와 회수 작업이 선점 수량 계산에 어떻게 반영되는지 설명한다
- 우려 신호: 낙관적 잠금을 '충돌하면 다시 시도한다'는 일반론으로만 설명한다 / TTL 만료 후 재고 복구 시점을 설명하지 않는다
- 근거: 맥락 연결 4f22f08f, 기준 R-08

#### Q7. 오류 코드나 검증 규칙을 새로 추가할 때 수정할 위치가 어디까지 퍼지는지, 지금 코드 기준으로 설명해 주시겠어요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:R-12`
- 의도: 오류 코드와 검증 규칙의 변경 지점이 한 곳에 모여 있는지 지원자가 스스로 판단하는지 확인한다.
- 꼬리 질문: 상태 코드와 오류 코드의 매핑은 어디에서 관리되고 있나요? / 도메인 규칙이 HTTP 요청·응답 객체에 의존하는 부분이 있나요? / 멱등성 저장을 다른 구현으로 바꿀 때 주문 생성 로직에서 고칠 곳이 있나요?
- 좋은 답변의 신호: 변경 지점을 모듈 단위로 특정해 설명한다. / 오류 코드 매핑의 위치를 명확히 말한다. / 도메인 규칙과 HTTP 처리의 의존 방향을 구분해 설명한다.
- 우려 신호: 변경 지점이 여러 곳에 흩어져 있음을 인식하지 못한다. / 도메인 규칙과 HTTP 계층의 의존 관계를 구분하지 않는다.
- 근거: 기준 R-12

#### Q8. 이 경험에서 본인이 맡은 범위와 가장 어려웠던 기술적 결정을 설명해 주시겠어요? <sub>이력서 연결 · OPTIONAL · 5분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서에 적힌 경험에서 지원자가 직접 맡은 범위와 기술적 판단의 근거를 확인한다.
- 꼬리 질문: 그 작업에서 본인이 직접 설계하거나 구현한 부분은 어디까지였나요? / 그 결정을 내릴 때 어떤 기준으로 대안을 비교하셨나요? / 지금 다시 한다면 어떤 부분을 다르게 선택하시겠어요?
- 좋은 답변의 신호: 본인이 맡은 범위와 팀이 맡은 범위를 구분해 설명한다 / 결정의 근거로 당시의 제약 조건과 비교한 대안을 구체적으로 든다
- 우려 신호: 맡은 범위를 물어도 팀 전체의 결과만 설명한다 / 결정의 이유를 물으면 기술 이름만 나열하고 적용 조건을 말하지 않는다
- 근거: 맥락 연결 924e5724, 기준 R-05

#### Q9. 운송장 조회 API의 p95 응답 시간을 1.2초에서 180ms로 단축했다고 하셨는데, 그 수치는 어떤 부하 조건에서 어떻게 측정한 값인가요? <sub>이력서 연결 · OPTIONAL · 5분 · OPERABILITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 성능 개선 수치의 측정 조건과 개선 수단을 본인이 설명할 수 있는지 확인한다.
- 꼬리 질문: p95는 어떤 도구로 어느 구간에서 측정했나요? / 인덱스 재설계와 캐시 도입이 각각 지연 개선에 얼마나 기여했나요? / 캐시 만료나 무효화는 어떻게 처리했나요?
- 좋은 답변의 신호: 측정 도구, 부하 조건, 측정 구간을 구체적으로 설명한다 / 인덱스와 캐시의 기여를 분리해 설명한다
- 우려 신호: 측정 방법을 설명하지 못한다 / 캐시 무효화 전략을 설명하지 않는다
- 근거: 맥락 연결 ef6ca64b

### taeyun · taeyun-order-api

#### Q1. R-06 실행 기록에서 같은 키로 다른 본문을 보낸 두 요청이 모두 201로 처리됐습니다. 주문 생성 경로에서 키와 본문을 비교하는 판단이 어디에서 이뤄지고 있는지 설명해 주시겠어요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-06`
- 의도: 멱등 키 충돌을 걸러내는 판단 지점을 지원자가 코드에서 어떻게 짚어내는지 확인한다.
- 꼬리 질문: 같은 키로 들어온 두 번째 요청이 처리되는 순서를 코드 흐름대로 말씀해 주시겠어요? / 키에 저장된 본문과 새 본문을 비교할 때 무엇을 기준으로 같다·다르다를 정하나요? / 충돌을 거절로 바꿀 때 재고 차감과 주문 저장 중 어느 시점에 검사를 두시겠어요?
- 좋은 답변의 신호: 키 조회와 본문 비교가 어느 단계에서 일어나는지 코드 위치를 들어 설명한다. / 충돌 판정을 재고 차감 이전에 두어야 하는 이유를 부작용 관점에서 말한다.
- 우려 신호: 키 존재 여부만 확인하고 본문 비교가 없다는 점을 짚지 못한다. / 충돌 응답을 만들 위치를 말하면서 기존 주문·재고에 미치는 영향을 언급하지 않는다.
- 근거: 기준 R-06, 실행 기록 db787431 · R-06, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23, 코드 src/routes/order-routes.ts:30-33

#### Q2. R-07 실행 기록에서 같은 키의 동시 요청 10건 중 5건만 201이고 응답 id도 서로 달랐으며 재고가 0까지 줄었습니다. 같은 키의 요청들이 겹칠 때 어떤 순서로 처리되는지 설명해 주시겠어요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-07`
- 의도: 동시 요청에서 키 기록과 재고 차감이 어떤 순서로 얽히는지 지원자가 분석하는지 확인한다.
- 꼬리 질문: 두 요청이 거의 동시에 들어오면 키 조회와 키 저장 사이에 어떤 일이 생기나요? / 재고 차감이 여러 번 일어난 경로를 실행 기록의 숫자로 어떻게 설명하시겠어요? / 동시성을 다루는 방식을 바꿀 때 처리량과 정합성 중 무엇을 기준으로 정하시겠어요?
- 좋은 답변의 신호: 키 조회와 저장 사이의 경합 구간을 짚고 재고 차감 횟수와 연결해 설명한다. / 동시성 제어를 도입할 때 비용과 정합성의 맞바꿈을 언급한다.
- 우려 신호: 동시 요청을 순차 요청과 같은 상황으로 설명한다. / 재고가 0이 된 이유를 키 처리와 연결하지 못한다.
- 근거: 기준 R-07, 실행 기록 7c641beb · R-07, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q3. R-03 재고 부족 거절 기준은 통과했습니다. 이 요구사항을 보장하는 코드가 어떻게 동작하는지 설명해 주시겠어요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · TEMPLATE</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 통과한 R-03 재고 부족 거절 기준을 지원자가 직접 설명할 수 있는지, 경계 조건에서 깨지는 조건까지 알고 있는지 확인한다.
- 꼬리 질문: 이 경우를 처리하는 코드는 어느 파일과 함수에 있나요? / 그 처리가 올바르게 동작하는 원리는 무엇인가요? / 다른 구현 방식과 비교하면 지금 방식의 장단점은 무엇인가요?
- 좋은 답변의 신호: 처리 코드의 위치를 스스로 찾고 동작 순서를 정확히 설명한다. / 이 처리가 깨지는 입력이나 동시 요청 조건을 스스로 말한다.
- 우려 신호: 통과한 결과만 말하고 코드의 동작 원리를 설명하지 않는다. / 다른 구현 방식과의 차이를 구체적으로 비교하지 않는다.
- 근거: 기준 R-03, 실행 기록 ce8791ed · R-03, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q4. 가장 긴 함수가 라우트 파일의 createOrderRoutes로 34줄이라는 신호가 있습니다. 이 함수가 맡은 일을 나열해 주시고, 그중 어디까지를 라우트 계층에 두기로 정했는지 설명해 주시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 라우트 함수에 섞인 책임을 지원자가 어떻게 구분하고 경계를 정하는지 확인한다.
- 꼬리 질문: 이 함수 안에서 요청 파싱과 도메인 판단이 각각 어디에서 이뤄지나요? / 도메인 규칙을 별도 모듈로 옮긴다면 어떤 함수가 남고 무엇이 빠지나요? / 계층을 나눌 때 얻는 것과 잃는 것을 어떤 기준으로 저울질하시겠어요?
- 좋은 답변의 신호: 함수 안의 단계를 나열하고 각 단계의 책임을 구분해 말한다. / 분리했을 때의 이득과 비용을 구체적인 변경 시나리오로 설명한다.
- 우려 신호: 함수 길이를 줄이는 것 자체를 목표로 말한다. / 요청·응답 객체에 도메인 규칙이 의존하는지 여부를 언급하지 않는다.
- 근거: 코드 src/routes/order-routes.ts:8-41, 기준 R-12

#### Q5. R-04 입력 검증 기준은 통과했습니다. 이 요구사항을 보장하는 코드가 어떻게 동작하는지 설명해 주시겠어요? <sub>강점 확인 · SHOULD · 6분 · ROBUSTNESS · TEMPLATE</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 통과한 R-04 입력 검증 기준을 지원자가 직접 설명할 수 있는지, 경계 조건에서 깨지는 조건까지 알고 있는지 확인한다.
- 꼬리 질문: 이 경우를 처리하는 코드는 어느 파일과 함수에 있나요? / 그 처리가 올바르게 동작하는 원리는 무엇인가요? / 다른 구현 방식과 비교하면 지금 방식의 장단점은 무엇인가요?
- 좋은 답변의 신호: 처리 코드의 위치를 스스로 찾고 동작 순서를 정확히 설명한다. / 이 처리가 깨지는 입력이나 동시 요청 조건을 스스로 말한다.
- 우려 신호: 통과한 결과만 말하고 코드의 동작 원리를 설명하지 않는다. / 다른 구현 방식과의 차이를 구체적으로 비교하지 않는다.
- 근거: 기준 R-04, 실행 기록 d0198e58 · R-04, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q6. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 멱등성과 재고 처리가 어떻게 달라질지 설명해 주시겠어요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인메모리 상태를 전제한 설계가 다중 인스턴스에서 어떤 한계를 갖는지 지원자가 인식하는지 확인한다.
- 꼬리 질문: 키 기록이 인스턴스마다 따로 존재할 때 같은 키의 두 요청은 어떻게 처리되나요? / 재고 차감이 여러 인스턴스에 나뉘면 어떤 값이 관측될 수 있나요? / 여러 인스턴스로 확장할 때 정합성과 응답 지연 중 무엇을 우선할지 어떤 기준으로 정하시겠어요?
- 좋은 답변의 신호: 인스턴스별 상태 분리가 멱등성과 재고에 미치는 영향을 구체적으로 말한다. / 확장 시 필요한 공유 지점을 짚고 그에 따르는 비용을 언급한다.
- 우려 신호: 인메모리 상태가 인스턴스마다 다르다는 점을 언급하지 않는다. / 확장을 코드 수정 없이 가능하다고 단정한다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q7. 이 경험에서 본인이 맡은 범위와 가장 어려웠던 기술적 결정을 설명해 주시겠어요? <sub>이력서 연결 · SHOULD · 5분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적힌 경험에서 지원자가 직접 맡은 범위와 기술적 판단의 근거를 확인한다.
- 꼬리 질문: 그 작업에서 본인이 직접 설계하거나 구현한 부분은 어디까지였나요? / 그 결정을 내릴 때 어떤 기준으로 대안을 비교하셨나요? / 지금 다시 한다면 어떤 부분을 다르게 선택하시겠어요?
- 좋은 답변의 신호: 본인이 맡은 범위와 팀이 맡은 범위를 구분해 설명한다 / 결정의 근거로 당시의 제약 조건과 비교한 대안을 구체적으로 든다
- 우려 신호: 맡은 범위를 물어도 팀 전체의 결과만 설명한다 / 결정의 이유를 물으면 기술 이름만 나열하고 적용 조건을 말하지 않는다
- 근거: 맥락 연결 28cd7ebc, 기준 R-05

#### Q8. R-12 설계 검토 항목을 함께 보겠습니다. 모듈과 책임을 나눈 기준은 무엇이었나요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · TEMPLATE</sub>

- 슬롯: `DESIGN_TRADEOFF:R-12`
- 의도: R-12 설계·변경 용이성 항목에서 구조를 정한 기준과 변경 비용에 대한 판단을 확인한다.
- 꼬리 질문: 이 코드가 맡고 있는 책임을 나누어 설명해 주시겠어요? / 요구사항이 하나 바뀌면 이 구조에서 어느 부분을 고치게 되나요? / 구조를 더 나누면 얻는 것과 잃는 것은 무엇인가요?
- 좋은 답변의 신호: 코드의 책임을 함수와 파일 단위로 나누어 설명한다. / 구조를 바꿀 때의 비용과 이득을 요구사항 변경 예시로 비교한다.
- 우려 신호: 구조를 정한 이유를 설명하지 않고 선호로만 답한다. / 변경 예시를 들 때 영향 범위를 짚지 않는다.
- 근거: 기준 R-12

#### Q9. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구현에서 가장 먼저 바꿔야 할 부분은 어디인가요? <sub>요구사항 확장 · OPTIONAL · 6분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 요구사항이 바뀌었을 때 현재 구현(R-01·R-02·R-05)에서 영향을 받는 부분과 필요한 보장을 판단하는지 확인한다.
- 꼬리 질문: 지금 구현에서 그 상황의 영향을 받는 상태는 무엇인가요? / 그 상태를 안전하게 다루려면 어떤 보장이 필요한가요? / 그 보장을 얻는 방법들 가운데 어떤 기준으로 하나를 고르시겠어요?
- 좋은 답변의 신호: 영향을 받는 상태와 코드 위치를 구체적으로 짚는다. / 선택지마다 비용과 보장 수준을 비교해 하나를 고른다.
- 우려 신호: 특정 기술 이름만 말하고 필요한 보장을 설명하지 않는다. / 현재 구현이 받는 영향을 구체적으로 짚지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q10. 회사 예약 API에서 같은 시간대 중복 예약을 막는 검증 로직은 어떤 조건으로 판정하도록 구현하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · REQUIREMENTS · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서의 중복 예약 검증이 어떤 경계 조건과 저장 구조를 전제로 했는지 확인한다.
- 꼬리 질문: 겹침 판정은 시작·종료 시각을 어떤 비교식으로 처리했나요? / 검증과 저장 사이에 다른 요청이 끼어들면 어떻게 되나요? / 개인 프로젝트의 인메모리 저장 방식과 회사 구현은 어떤 차이가 있었나요?
- 좋은 답변의 신호: 겹침 판정의 경계 조건(끝점 포함 여부)을 명확히 설명한다 / 검증 시점과 저장 시점의 순서, 트랜잭션 범위를 설명한다 / 회사 구현과 개인 프로젝트의 저장 구조 차이를 구분해 설명한다
- 우려 신호: 겹침 판정을 '시간이 겹치면 거절'로만 설명한다 / 검증과 저장 사이의 경합 가능성을 고려하지 않는다 / 경계 조건(끝점)을 설명하지 못한다
- 근거: 맥락 연결 7ddc9e41, 기준 R-03

#### Q11. 라우팅·서비스·저장소 계층을 분리한 리팩터링에서 계층 간 경계는 어떤 기준으로 나누셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 이력서의 계층 분리 경험이 실제로 어떤 책임 분담과 변경 범위 축소로 이어졌는지 확인한다.
- 꼬리 질문: 각 계층이 책임지는 것과 책임지지 않는 것은 무엇이었나요? / 신규 기능을 추가할 때 실제로 몇 개 계층을 수정하게 되었나요? / 계층 분리 이후 테스트는 어느 계층에 두었나요?
- 좋은 답변의 신호: 계층별 책임과 의존 방향을 구체적으로 설명한다 / 리팩터링 전후 변경 범위를 사례와 함께 설명한다 / 테스트 배치 기준을 계층과 연결해 설명한다
- 우려 신호: 계층을 '폴더로 나눴다'는 수준으로만 설명한다 / 변경 범위가 줄었는지 사례를 들지 못한다 / 계층 간 의존 방향을 설명하지 못한다
- 근거: 맥락 연결 089bb6b7, 기준 R-12

### gaeun · gaeun-order-api

#### Q1. R-05 실행 기록에서 같은 키로 두 번 보냈을 때 두 번째 응답의 id가 첫 번째와 달랐고 재고도 두 번 줄었습니다. 주문 생성 요청이 들어왔을 때 멱등성 기록을 어떤 순서로 확인하고 갱신하도록 코드를 구성하셨나요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-05`
- 의도: 멱등 재전송에서 같은 키를 어떻게 조회·기록하는지, 그 순서가 중복 주문과 중복 차감을 어떻게 막는지 설명하는지 확인한다.
- 꼬리 질문: POST /orders 처리 흐름에서 키를 조회하는 시점과 기록을 남기는 시점은 각각 어디인가요? / 재고 차감과 멱등성 기록 저장이 서로 다른 시점에 일어날 때 어떤 불일치가 생길 수 있나요? / 기록을 남기는 시점을 앞당기거나 늦출 때 각각 어떤 비용이 생기나요?
- 좋은 답변의 신호: 키 조회와 기록 저장의 순서를 코드 위치와 함께 구체적으로 설명한다. / 두 번째 호출이 새 주문을 만들지 않으려면 무엇을 먼저 확인해야 하는지 스스로 짚는다.
- 우려 신호: 재고 차감만 언급하고 멱등성 기록의 조회·저장 시점은 말하지 않는다. / 응답 id가 달라진 이유를 코드 흐름과 연결하지 못한다.
- 근거: 기준 R-05, 실행 기록 9dc6589f · R-05, 코드 src/app.ts:23-31, 코드 src/app.ts:43-69, 코드 src/app.ts:33-41

#### Q2. R-06 실행 기록에서는 같은 키에 다른 본문을 보냈을 때 201과 409가 돌아왔고 재고도 함께 줄었습니다. 같은 키로 들어온 요청의 본문이 이전과 다를 때 어떤 값을 비교해 어떻게 판단하도록 작성하셨나요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-06`
- 의도: 키 충돌 판정에서 무엇을 비교 대상으로 삼았는지, 충돌 시 새 주문과 재고 변경을 어떻게 막는지 확인한다.
- 꼬리 질문: 저장해 둔 요청 내용과 새 요청을 비교할 때 어떤 필드를 기준으로 삼았나요? / 본문 비교를 어느 시점에 수행하면 재고 차감 전에 충돌을 가려낼 수 있나요? / 본문을 그대로 저장하는 방식과 해시로 저장하는 방식의 차이는 무엇인가요?
- 좋은 답변의 신호: 충돌 판정에 쓰는 비교 대상과 그 판정 위치를 코드 흐름으로 설명한다. / 충돌로 끝난 요청이 재고와 주문 상태를 바꾸지 않아야 하는 이유를 말한다.
- 우려 신호: 충돌 판정 없이 재고 검사만 먼저 수행한 흐름을 그대로 답한다. / 다른 본문이 왜 다른 오류 코드로 이어져야 하는지 설명하지 못한다.
- 근거: 기준 R-06, 실행 기록 d9d1a193 · R-06, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:33-41

#### Q3. 제출한 테스트는 기준 상태에서 통과했지만 재고 부족 검사를 없앤 변형을 잡아내지 못했습니다. 재고 부족과 수량 경계를 검증하는 테스트를 어떤 입력과 기대값으로 설계하셨나요? <sub>테스트 설계 · MUST · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G1`
- 의도: 경계·검증 규칙을 겨냥한 테스트 케이스를 어떤 입력과 단언으로 구성했는지, 변형을 잡아낼 만큼 촘촘한지 확인한다.
- 꼬리 질문: 재고와 같은 수량, 재고보다 큰 수량, 0과 음수 수량을 각각 어떤 케이스로 다루었나요? / 재고가 변하지 않았다는 사실을 어떤 조회로 단언하나요? / 검증 규칙이 사라졌을 때 실패할 단언을 지금 테스트에 추가한다면 무엇을 넣겠어요?
- 좋은 답변의 신호: 경계값과 그 기대 상태 코드를 케이스별로 구분해 설명한다. / 재고 불변을 확인하는 단언을 테스트에 포함해 말한다.
- 우려 신호: 정상 주문 성공 케이스만 언급하고 거절 케이스는 말하지 않는다. / 단언 없이 상태 코드만 확인하는 수준에 머문다.
- 근거: 기준 G1, 변이 M-01, 코드 src/app.ts:61-61

#### Q4. R-03은 재고 부족과 재고 0, 재고와 같은 수량 주문까지 모두 기대대로 통과했습니다. 재고와 요청 수량을 비교하는 조건을 어떤 기준으로 정하셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 재고 부족 판정의 비교 기준과 경계 포함 여부를 어떤 근거로 정했는지 확인한다.
- 꼬리 질문: 재고와 같은 수량을 허용한 이유는 무엇인가요? / 거절로 끝난 요청에서 재고를 건드리지 않으려면 차감을 어디에 두어야 하나요? / 재고 확인과 차감 사이에 다른 요청이 끼어들면 어떤 문제가 생기나요?
- 좋은 답변의 신호: 비교 연산의 경계 포함 여부를 명시적으로 설명한다. / 거절 경로에서 재고 변경이 일어나지 않는 구조를 코드 흐름으로 말한다.
- 우려 신호: 경계값을 우연히 맞춘 듯 설명하지 못한다. / 거절과 차감의 순서를 구분하지 않는다.
- 근거: 기준 R-03, 실행 기록 e23d429c · R-03, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31

#### Q5. R-09 실행 기록에서는 취소 후 재고가 시드 값으로 돌아오지 않고 1에 머물렀습니다. 취소 요청을 처리할 때 주문 상태와 재고를 어떤 순서로 갱신하도록 작성하셨나요? <sub>실패 디브리핑 · SHOULD · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-09`
- 의도: 취소 처리에서 상태 변경과 재고 복구의 순서, 중복 취소 방지 조건을 어떻게 구성했는지 확인한다.
- 꼬리 질문: 취소 가능 여부를 판단하는 조건은 무엇인가요? / 이미 취소된 주문에 다시 취소가 들어오면 재고 복구가 두 번 일어나지 않게 어떻게 막나요? / 존재하지 않는 주문 id의 취소는 어느 지점에서 걸러지나요?
- 좋은 답변의 신호: 상태 확인과 재고 복구의 순서를 코드 위치와 함께 설명한다. / 중복 취소 시 복구가 한 번만 일어나야 하는 이유를 말한다.
- 우려 신호: 재고 복구 로직이 실행되지 않은 이유를 코드에서 찾지 못한다. / 중복 취소와 미존재 주문 처리를 같은 조건으로 뭉뚱그린다.
- 근거: 기준 R-09, 실행 기록 a07f6a1b · R-09, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:71-87, 코드 src/app.ts:33-41

#### Q6. 가장 긴 함수가 POST /orders 콜백으로 27줄이라는 신호가 있습니다. 이 콜백 안에서 요청 파싱, 검증, 재고 차감, 멱등성 처리를 어떤 기준으로 나누거나 합치셨나요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: HTTP 처리와 도메인 규칙의 경계를 어떤 기준으로 나눴는지, 그 판단의 근거를 확인한다.
- 꼬리 질문: 이 콜백에서 HTTP와 무관한 규칙은 어디에 두었나요? / 규칙을 별도 함수로 빼면 어떤 점이 달라지나요? / 지금 구조에서 새 검증 규칙을 추가하면 어느 파일을 고치게 되나요?
- 좋은 답변의 신호: 콜백이 담당하는 책임과 도메인 규칙의 경계를 구분해 설명한다. / 분리했을 때의 이득과 비용을 함께 말한다.
- 우려 신호: 모든 처리가 한 함수에 있는 이유를 설명하지 못한다. / 도메인 규칙이 요청·응답 객체에 의존하는지 여부를 인식하지 못한다.
- 근거: 코드 src/app.ts:43-69, 기준 R-12

#### Q7. R-08은 서로 다른 키 10건 동시 요청에서 201 5건과 409 5건, 재고 0으로 기대대로 통과했습니다. 동시에 들어온 요청들 사이에서 재고 확인과 차감이 어긋나지 않게 어떤 방식으로 처리하셨나요? <sub>강점 확인 · SHOULD · 6분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-08`
- 의도: 동시 요청에서 재고 하한을 지키기 위해 어떤 직렬화나 원자적 처리를 택했는지 확인한다.
- 꼬리 질문: 재고 확인과 차감 사이에 다른 요청이 끼어들 수 있는 지점은 어디인가요? / 차감을 원자적으로 만들기 위해 어떤 언어 기능을 썼나요? / 요청 수가 늘어날 때 이 방식의 처리량은 어떻게 달라지나요?
- 좋은 답변의 신호: 확인과 차감 사이의 경합 지점을 정확히 짚는다. / 동시성 제어 방식과 그 한계를 함께 설명한다.
- 우려 신호: 단일 스레드라 문제가 없다는 말로만 끝낸다. / 재고가 음수가 될 수 있는 경로를 인식하지 못한다.
- 근거: 기준 R-08, 실행 기록 52a00cd4 · R-08, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:33-41

#### Q8. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어오는 상황을 가정해 보겠습니다. 지금 구현에서 가장 먼저 바꿔야 할 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 요구사항이 바뀌었을 때 현재 구현(R-01·R-05·R-06)에서 영향을 받는 부분과 필요한 보장을 판단하는지 확인한다.
- 꼬리 질문: 지금 구현에서 그 상황의 영향을 받는 상태는 무엇인가요? / 그 상태를 안전하게 다루려면 어떤 보장이 필요한가요? / 그 보장을 얻는 방법들 가운데 어떤 기준으로 하나를 고르시겠어요?
- 좋은 답변의 신호: 영향을 받는 상태와 코드 위치를 구체적으로 짚는다. / 선택지마다 비용과 보장 수준을 비교해 하나를 고른다.
- 우려 신호: 특정 기술 이름만 말하고 필요한 보장을 설명하지 않는다. / 현재 구현이 받는 영향을 구체적으로 짚지 않는다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q9. 팀 프로젝트에서 멱등성 키를 도입해 같은 요청이 두 번 처리되지 않도록 설계하셨다고 하셨는데, 그때 키와 응답을 어디에 어떤 조건으로 저장하도록 정하셨나요? <sub>이력서 연결 · SHOULD · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적힌 멱등성 설계 경험의 저장 위치·만료·충돌 처리 조건을 구체적으로 확인한다.
- 꼬리 질문: 같은 키로 다른 본문이 들어오면 어떤 응답을 주도록 정했나요? / 같은 키의 요청이 동시에 두 번 도착할 때 어느 지점에서 하나로 정리되나요? / 그 설계에서 보장하려던 범위와 포기한 부분은 무엇이었나요?
- 좋은 답변의 신호: 키 저장 위치와 보관 기간, 본문 비교 기준을 구체적으로 설명한다 / 동시 요청을 막는 지점(유일 제약·잠금)과 재시도 조건을 구분해 설명한다
- 우려 신호: 멱등성을 '같은 요청은 한 번만 처리한다'는 일반론으로만 설명한다 / 키 충돌 시 응답 코드나 본문 비교 기준을 제시하지 못한다
- 근거: 맥락 연결 8a3e3b3f, 기준 R-05

#### Q10. R-04 실행 기록에서는 수량 누락·0·음수 같은 요청이 400이 아니라 201로 처리되었습니다. 주문 생성 전에 입력을 검사하는 절차를 어떤 순서와 기준으로 두셨나요? <sub>실패 디브리핑 · OPTIONAL · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-04`
- 의도: 검증 항목과 검증 시점을 어떻게 배치했는지, 검증 실패가 멱등성 기록과 재고에 남지 않는지 확인한다.
- 꼬리 질문: 수량과 상품 id, 헤더 중 무엇을 먼저 검사하나요? / 검증 실패로 끝난 요청의 키를 기록에 남기지 않으려면 어느 시점에 기록해야 하나요? / JSON이 아닌 본문은 어느 단계에서 걸러지나요?
- 좋은 답변의 신호: 검증 항목과 순서를 구체적으로 나열한다. / 검증 실패 요청이 상태를 바꾸지 않아야 하는 이유를 말한다.
- 우려 신호: 검증을 재고 확인 뒤에 두는 흐름을 그대로 답한다. / 실패한 요청의 키가 기록에 남는지 여부를 고려하지 않는다.
- 근거: 기준 R-04, 실행 기록 c0856d5f · R-04, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31

#### Q11. 코드에 명시적 any가 3곳 있다는 신호가 있습니다. 이 지점들에서 타입을 any로 둔 이유와, 대신 쓸 수 있었던 타입 정의를 어떻게 판단하셨나요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:explicit-any`
- 의도: any를 선택한 근거와 타입 경계 설계를 어떻게 판단했는지 확인한다.
- 꼬리 질문: 이 위치에서 타입을 좁히려면 어떤 정보가 더 필요했나요? / any를 남겨 둘 때 이후 변경에서 어떤 위험이 생기나요? / 요청 본문 타입을 어디에 정의하면 검증과 타입이 함께 맞춰지나요?
- 좋은 답변의 신호: any를 둔 지점과 이유를 구체적으로 설명한다. / 타입 정의 위치와 검증 규칙의 관계를 말한다.
- 우려 신호: 타입 오류를 피하려고 any를 썼다고만 답한다. / 해당 위치가 어디인지 짚지 못한다.
- 근거: 코드 src/app.ts:44-44, 코드 src/app.ts:90-90, 코드 src/data.ts:42-42, 기준 R-12

#### Q12. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구현에서 가장 먼저 바꿔야 할 부분은 어디인가요? <sub>요구사항 확장 · OPTIONAL · 6분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 요구사항이 바뀌었을 때 현재 구현(R-01·R-02·R-05)에서 영향을 받는 부분과 필요한 보장을 판단하는지 확인한다.
- 꼬리 질문: 지금 구현에서 그 상황의 영향을 받는 상태는 무엇인가요? / 그 상태를 안전하게 다루려면 어떤 보장이 필요한가요? / 그 보장을 얻는 방법들 가운데 어떤 기준으로 하나를 고르시겠어요?
- 좋은 답변의 신호: 영향을 받는 상태와 코드 위치를 구체적으로 짚는다. / 선택지마다 비용과 보장 수준을 비교해 하나를 고른다.
- 우려 신호: 특정 기술 이름만 말하고 필요한 보장을 설명하지 않는다. / 현재 구현이 받는 영향을 구체적으로 짚지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q13. 이 경험에서 본인이 맡은 범위와 가장 어려웠던 기술적 결정을 설명해 주시겠어요? <sub>이력서 연결 · OPTIONAL · 5분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서에 적힌 경험에서 지원자가 직접 맡은 범위와 기술적 판단의 근거를 확인한다.
- 꼬리 질문: 그 작업에서 본인이 직접 설계하거나 구현한 부분은 어디까지였나요? / 그 결정을 내릴 때 어떤 기준으로 대안을 비교하셨나요? / 지금 다시 한다면 어떤 부분을 다르게 선택하시겠어요?
- 좋은 답변의 신호: 본인이 맡은 범위와 팀이 맡은 범위를 구분해 설명한다 / 결정의 근거로 당시의 제약 조건과 비교한 대안을 구체적으로 든다
- 우려 신호: 맡은 범위를 물어도 팀 전체의 결과만 설명한다 / 결정의 이유를 물으면 기술 이름만 나열하고 적용 조건을 말하지 않는다
- 근거: 맥락 연결 7dc08579, 기준 R-04

#### Q14. 북마크 API의 테스트는 어떤 입력과 기대 결과를 기준으로 작성하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · TESTING · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 이력서에 적힌 테스트 코드의 검증 범위와 케이스 선정 기준을 확인한다.
- 꼬리 질문: 정상 경로 외에 어떤 실패 케이스를 테스트에 넣었나요? / 테스트가 통과해도 실제 결함이 남을 수 있는 부분은 어디라고 보시나요? / 인메모리 저장소를 쓰는 테스트에서 상태 초기화는 어떻게 처리했나요?
- 좋은 답변의 신호: 경계값과 실패 케이스를 구체적으로 열거한다 / 테스트가 검증하지 못하는 범위를 스스로 구분해 설명한다
- 우려 신호: 정상 경로 위주의 테스트만 언급한다 / 테스트 통과를 구현 정확성의 근거로만 설명한다
- 근거: 맥락 연결 108ba4f5, 기준 G1

### dohyun · dohyun-order-api

#### Q1. 재고 부족으로 주문이 거절되는 경로를 구현하셨는데, 재고와 요청 수량을 비교하는 판단을 어디에 두고 어떤 기준으로 정하셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 재고 부족 거절 판단의 위치와 기준, 재고를 변동시키지 않는 처리 방식을 확인한다.
- 꼬리 질문: 재고와 같은 수량의 주문이 통과하는 경계는 코드에서 어떻게 표현되어 있나요? / 재고 확인과 차감 사이에 다른 요청이 끼어들면 어떤 일이 생기나요? / 재고 부족과 입력 오류를 같은 상태 코드로 묶지 않은 이유는 무엇인가요?
- 좋은 답변의 신호: 재고 비교 조건과 차감 시점을 구분해 설명한다. / 거절 시 재고를 건드리지 않는 흐름을 코드 순서로 말한다. / 경계값(재고와 같은 수량)을 직접 언급한다.
- 우려 신호: 재고 확인과 차감이 한 곳에서 뒤섞여 있다고만 답한다. / 거절 경로에서 재고가 변하지 않는 이유를 설명하지 못한다.
- 근거: 기준 R-03, 실행 기록 168234be · R-03, 코드 src/index.ts:101-233, 코드 src/index.ts:78-92

#### Q2. 주문 생성 요청의 입력 검증을 구현하셨는데, 어떤 항목을 어떤 순서로 검증하고 검증 실패 시 상태를 어떻게 남기지 않도록 하셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 검증 항목의 범위와 순서, 실패 요청이 상태나 멱등성 기록에 남지 않게 하는 처리를 확인한다.
- 꼬리 질문: 수량의 타입과 범위를 판정하는 조건은 어떻게 작성하셨나요? / 헤더 누락과 본문 형식 오류를 같은 오류 코드로 묶은 기준은 무엇인가요? / 검증 실패 요청의 키가 이후 재사용될 때 어떤 일이 일어나나요?
- 좋은 답변의 신호: 검증 항목을 나열하고 각 판정 조건을 구체적으로 말한다. / 실패 요청이 멱등성 기록에 남지 않는 이유를 설명한다. / 검증 순서를 정한 기준을 말한다.
- 우려 신호: 검증 항목 일부만 언급하고 나머지는 확인하지 않는다. / 검증 실패 시 재고나 기록에 미치는 영향을 말하지 못한다.
- 근거: 기준 R-04, 실행 기록 9dcb336b · R-04, 코드 src/index.ts:101-233, 코드 src/index.ts:78-92

#### Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 멱등성과 재고 일관성을 유지하려면 무엇이 달라져야 한다고 보시나요? <sub>요구사항 확장 · MUST · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인메모리 상태를 공유하지 않는 다중 인스턴스 환경에서의 일관성 확보 방안과 그 비용을 판단하는지 확인한다.
- 꼬리 질문: 인스턴스마다 상태가 따로 있으면 같은 키의 재전송이 어떻게 처리되나요? / 재고 차감을 인스턴스 간에 조율하려면 어떤 지점을 바꿔야 하나요? / 그 변경이 지금 구조에 주는 비용과 이점은 무엇인가요?
- 좋은 답변의 신호: 상태 공유 지점과 조율이 필요한 연산을 구분해 설명한다. / 확장 시 바꿔야 할 모듈 경계를 짚는다. / 일관성과 지연·복잡도 사이의 비용을 언급한다.
- 우려 신호: 인메모리 상태를 그대로 두면 된다고만 답한다. / 멱등성과 재고 중 한쪽만 언급하고 다른 쪽은 다루지 않는다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q4. 재고 부족 거절과 수량 경계를 검증하는 테스트를 작성하셨는데, 그 테스트가 어떤 입력을 다루고 어떤 결과를 확인하도록 설계하셨나요? <sub>테스트 설계 · SHOULD · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G1`
- 의도: 경계·검증 규칙을 겨냥한 테스트의 입력 선택과 단언 범위를 확인한다.
- 꼬리 질문: 재고와 같은 수량, 재고보다 많은 수량, 0과 음수 수량을 각각 어떻게 다루나요? / 상태 코드 외에 재고 값이나 오류 코드도 단언하나요? / 검증 규칙이 바뀌면 어떤 테스트가 먼저 실패하도록 설계하셨나요?
- 좋은 답변의 신호: 경계값을 입력으로 명시하고 단언 대상을 구체적으로 말한다. / 상태 코드와 부수 효과(재고)를 함께 단언한다. / 규칙 변경 시 실패할 테스트를 지목한다.
- 우려 신호: 정상 경로 위주로만 테스트를 설명한다. / 단언 대상을 상태 코드 하나로만 한정한다.
- 근거: 기준 G1, 변이 M-01, 변이 M-02, 코드 src/index.ts:202-202, 코드 src/index.ts:157-157

#### Q5. src/index.ts:32-34에 플래그 조건을 반복하며 기다리는 코드가 있는데, 이 대기가 필요한 이유와 그 자리에서 기다리는 방식을 택한 기준을 설명해 주시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:busy-waits`
- 의도: 대기 루프가 필요한 상황과 그 구현 선택의 근거, 대안과의 트레이드오프를 판단하는지 확인한다.
- 꼬리 질문: 이 대기가 끝나는 조건은 무엇이고 누가 그 조건을 바꾸나요? / 대기 중 다른 요청이 들어오면 어떤 일이 생기나요? / 다른 동기화 방식과 비교해 이 방식을 택한 이유는 무엇인가요?
- 좋은 답변의 신호: 대기가 걸리는 조건과 해제 조건을 구체적으로 설명한다. / 대안 방식과의 비용 차이를 언급한다. / 동시 요청에서의 동작을 설명한다.
- 우려 신호: 대기가 왜 필요한지 설명하지 못한다. / 대기 중 다른 요청의 영향을 고려하지 않는다.
- 근거: 코드 src/index.ts:32-34, 기준 R-12

#### Q6. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구현에서 가장 먼저 바꿔야 할 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 요구사항이 바뀌었을 때 현재 구현(R-01·R-02·R-05)에서 영향을 받는 부분과 필요한 보장을 판단하는지 확인한다.
- 꼬리 질문: 지금 구현에서 그 상황의 영향을 받는 상태는 무엇인가요? / 그 상태를 안전하게 다루려면 어떤 보장이 필요한가요? / 그 보장을 얻는 방법들 가운데 어떤 기준으로 하나를 고르시겠어요?
- 좋은 답변의 신호: 영향을 받는 상태와 코드 위치를 구체적으로 짚는다. / 선택지마다 비용과 보장 수준을 비교해 하나를 고른다.
- 우려 신호: 특정 기술 이름만 말하고 필요한 보장을 설명하지 않는다. / 현재 구현이 받는 영향을 구체적으로 짚지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q7. 이 경험에서 본인이 맡은 범위와 가장 어려웠던 기술적 결정을 설명해 주시겠어요? <sub>이력서 연결 · SHOULD · 5분 · TRADEOFFS · TEMPLATE</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적힌 경험에서 지원자가 직접 맡은 범위와 기술적 판단의 근거를 확인한다.
- 꼬리 질문: 그 작업에서 본인이 직접 설계하거나 구현한 부분은 어디까지였나요? / 그 결정을 내릴 때 어떤 기준으로 대안을 비교하셨나요? / 지금 다시 한다면 어떤 부분을 다르게 선택하시겠어요?
- 좋은 답변의 신호: 본인이 맡은 범위와 팀이 맡은 범위를 구분해 설명한다 / 결정의 근거로 당시의 제약 조건과 비교한 대안을 구체적으로 든다
- 우려 신호: 맡은 범위를 물어도 팀 전체의 결과만 설명한다 / 결정의 이유를 물으면 기술 이름만 나열하고 적용 조건을 말하지 않는다
- 근거: 맥락 연결 dde624fb, 기준 G1

#### Q8. 멱등 재전송과 키 충돌을 검증하는 테스트를 작성하셨는데, 그 테스트가 어떤 호출 순서와 단언으로 두 동작을 구분하도록 설계하셨나요? <sub>테스트 설계 · OPTIONAL · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G2`
- 의도: 멱등 재전송과 충돌을 구분하는 테스트의 호출 순서와 단언 범위를 확인한다.
- 꼬리 질문: 같은 키·같은 본문 재전송에서 응답 id 외에 무엇을 단언하나요? / 같은 키·다른 본문 요청에서 재고가 변하지 않았음을 어떻게 확인하나요? / 첫 호출이 실패한 뒤 같은 키를 다시 쓰는 경우도 다루나요?
- 좋은 답변의 신호: 호출 순서를 명시하고 각 단계의 단언을 구분해 말한다. / 응답 id와 재고 변화를 함께 단언한다. / 충돌 시 새 주문이 생기지 않음을 확인하는 방법을 말한다.
- 우려 신호: 재전송 테스트만 언급하고 충돌 테스트는 다루지 않는다. / 단언 대상을 응답 상태 코드로만 한정한다.
- 근거: 기준 G2, 변이 M-03, 변이 M-04, 코드 src/index.ts:170-170, 코드 src/index.ts:177-177

#### Q9. src/index.ts:101-233의 주문 생성 핸들러가 133줄로 가장 긴 함수인데, 이 안에서 어떤 책임들을 함께 처리하고 있는지 설명해 주시겠어요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 긴 핸들러에 모인 책임의 종류와 분리 가능성에 대한 인식을 확인한다.
- 꼬리 질문: 이 함수 안에서 HTTP 처리와 도메인 규칙이 각각 어디에 있나요? / 새 오류 코드를 추가할 때 이 함수에서 고칠 곳은 몇 군데인가요? / 이 함수를 나눈다면 어떤 경계로 나누시겠어요?
- 좋은 답변의 신호: 함수 안의 책임을 종류별로 구분해 설명한다. / 변경 시 수정 지점을 구체적으로 짚는다. / 분리 경계를 도메인 규칙 기준으로 제안한다.
- 우려 신호: 함수가 길다는 사실만 인정하고 책임 구분을 말하지 못한다. / 변경 지점이 흩어져 있음을 인식하지 못한다.
- 근거: 코드 src/index.ts:101-233, 기준 R-12

#### Q10. REST API를 설계할 때 변경이 잦은 요구사항을 어떻게 반영할 수 있도록 구조를 잡으셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: REST API 설계에서 변경 용이성을 어떻게 확보했는지 확인한다.
- 꼬리 질문: API 스펙이 바뀔 때 어떤 부분을 먼저 수정하게 되나요? / 설계를 문서화하거나 리뷰하는 절차가 있었나요? / 변경이 잦은 프로젝트에서 설계를 단순하게 유지한 사례가 있나요?
- 좋은 답변의 신호: 변경 지점과 영향 범위를 구분해 설명한다 / 설계 문서나 리뷰 절차를 구체적으로 언급한다 / 변경 용이성을 위한 구조적 선택을 예로 든다
- 우려 신호: 설계 변경 대응 경험을 일반론으로만 말한다 / 구조적 선택의 이유를 설명하지 못한다 / 설계 문서화나 리뷰 경험을 말하지 못한다
- 근거: 맥락 연결 c6b99544, 기준 R-12

#### Q11. 매출 CSV 집계 배치에서 입력 파일 형식이 예상과 다르거나 값이 비어 있을 때 어떻게 처리하도록 설계하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · ROBUSTNESS · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 배치 스크립트의 입력 검증과 오류 처리 방식을 확인한다.
- 꼬리 질문: CSV 파싱을 직접 구현하면서 어떤 형식 오류를 처리 대상으로 삼았나요? / 집계 중 예외가 발생하면 이미 생성된 출력 파일은 어떻게 되나요? / 외부 라이브러리 없이 구현한 선택의 장단점은 무엇이었나요?
- 좋은 답변의 신호: 입력 검증 범위와 예외 처리 방식을 구체적으로 설명한다 / 실패 시 출력 파일 상태를 어떻게 관리하는지 설명한다 / 라이브러리 미사용 선택의 트레이드오프를 설명한다
- 우려 신호: 형식 오류 처리를 고려하지 않았다고 답한다 / 집계 실패 시 동작을 설명하지 못한다 / 구현 선택의 이유를 설명하지 못한다
- 근거: 맥락 연결 08d8214f

## 면접관 검토표 (사람 확인)

기술 면접관이 필수 질문을 읽고 "그대로 면접에 쓸 수 있는가"를 질문별로 표시한다. 고칠 점이 있으면 메모에 적는다.

| 페르소나 | 질문                                                                                                                                                                                                           | 유형          | 그대로 쓸 수 있는가 (예/아니오) | 메모 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------- | ---- |
| seojin   | Q1. 재고 부족으로 주문이 거절되는 경로를 구현하셨는데, 재고 확인과 차감이 어느 시점에 어떤 순서로 일어나는지 설명해 주시겠어요?                                                                                | 강점 확인     |                                 |      |
| seojin   | Q2. 주문 생성 요청의 입력 검증을 구현하셨는데, 어떤 입력들을 어떤 기준으로 걸러내는지 설명해 주시겠어요?                                                                                                       | 강점 확인     |                                 |      |
| seojin   | Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어오는 상황을 가정해 보겠습니다. 지금 구조에서 어떤 지점이 가장 먼저 문제가 될지 어떻게 판단하시겠어요?                                   | 요구사항 확장 |                                 |      |
| taeyun   | Q1. R-06 실행 기록에서 같은 키로 다른 본문을 보낸 두 요청이 모두 201로 처리됐습니다. 주문 생성 경로에서 키와 본문을 비교하는 판단이 어디에서 이뤄지고 있는지 설명해 주시겠어요?                                | 실패 디브리핑 |                                 |      |
| taeyun   | Q2. R-07 실행 기록에서 같은 키의 동시 요청 10건 중 5건만 201이고 응답 id도 서로 달랐으며 재고가 0까지 줄었습니다. 같은 키의 요청들이 겹칠 때 어떤 순서로 처리되는지 설명해 주시겠어요?                         | 실패 디브리핑 |                                 |      |
| taeyun   | Q3. R-03 재고 부족 거절 기준은 통과했습니다. 이 요구사항을 보장하는 코드가 어떻게 동작하는지 설명해 주시겠어요?                                                                                                | 강점 확인     |                                 |      |
| gaeun    | Q1. R-05 실행 기록에서 같은 키로 두 번 보냈을 때 두 번째 응답의 id가 첫 번째와 달랐고 재고도 두 번 줄었습니다. 주문 생성 요청이 들어왔을 때 멱등성 기록을 어떤 순서로 확인하고 갱신하도록 코드를 구성하셨나요? | 실패 디브리핑 |                                 |      |
| gaeun    | Q2. R-06 실행 기록에서는 같은 키에 다른 본문을 보냈을 때 201과 409가 돌아왔고 재고도 함께 줄었습니다. 같은 키로 들어온 요청의 본문이 이전과 다를 때 어떤 값을 비교해 어떻게 판단하도록 작성하셨나요?           | 실패 디브리핑 |                                 |      |
| gaeun    | Q3. 제출한 테스트는 기준 상태에서 통과했지만 재고 부족 검사를 없앤 변형을 잡아내지 못했습니다. 재고 부족과 수량 경계를 검증하는 테스트를 어떤 입력과 기대값으로 설계하셨나요?                                  | 테스트 설계   |                                 |      |
| gaeun    | Q4. R-03은 재고 부족과 재고 0, 재고와 같은 수량 주문까지 모두 기대대로 통과했습니다. 재고와 요청 수량을 비교하는 조건을 어떤 기준으로 정하셨나요?                                                              | 강점 확인     |                                 |      |
| dohyun   | Q1. 재고 부족으로 주문이 거절되는 경로를 구현하셨는데, 재고와 요청 수량을 비교하는 판단을 어디에 두고 어떤 기준으로 정하셨나요?                                                                                | 강점 확인     |                                 |      |
| dohyun   | Q2. 주문 생성 요청의 입력 검증을 구현하셨는데, 어떤 항목을 어떤 순서로 검증하고 검증 실패 시 상태를 어떻게 남기지 않도록 하셨나요?                                                                             | 강점 확인     |                                 |      |
| dohyun   | Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 멱등성과 재고 일관성을 유지하려면 무엇이 달라져야 한다고 보시나요?                         | 요구사항 확장 |                                 |      |
