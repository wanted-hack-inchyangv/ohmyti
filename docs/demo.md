# 90초 데모 시나리오 (T-505)

이 문서는 PRD 7장 표의 다섯 구간을 실제 화면의 URL·클릭 순서·기대 화면으로 옮겼다. 화면에 나오는 결과와 숫자는 모두
`pnpm demo:seed`가 실제 파이프라인(저장소 확인 → 실행 준비 → 요구사항 검증 → 테스트 실효성 → 리뷰 작성 → 맥락 연결)으로
만든 값이다. 화면은 이 저장된 값을 옮기기만 한다. 사전 계산 결과는 어디서나 `저장된 실행 · <시각>`으로 표시한다.

## 준비

로컬:

```bash
pnpm db:migrate
pnpm demo:seed          # 샘플 과제 승인 확인 → 샘플 A/B/C/D 제출 → 워커가 전체 파이프라인 실행 → 행 존재 확인
pnpm stack:local        # 웹(4310) + 워커(4320). 데모 중 "새로 실행"까지 보이려면 워커가 떠 있어야 한다
```

배포 환경(Vercel 웹 + Railway 워커): 배포 DB·Blob 스토어를 가리키는 환경변수로

```bash
pnpm demo:seed --env-store --external-worker
```

- `demo:seed`는 샘플마다 새 제출을 만든다. 처음에는 공개 샘플 저장소(`samples/order-api/sample-repos.json`)의 고정 커밋을
  워커가 GitHub에서 수집하고, 그 스냅샷을 `demo/samples/<id>/`에 저장해 두었다가 다음부터는 GitHub 없이 재사용한다.
- 예시 이력서는 `samples/order-api/demo/resume.txt`(가상 인물 Jordan Lee)를 PDF로 만들었다. GitHub 프로필은 넣지 않으므로
  GitHub 근거 탭은 `자료 없음`(프로필 미입력)이다.
- LLM(리뷰 작성·맥락 연결·mutation 위치 제안)은 워커 환경에 `DEEP_SEEK_API_KEY`가 있을 때 실제 DeepSeek로 돈다. 문장은 실행마다
  다를 수 있고, 판정·점수는 LLM과 무관하다(G-01).
- 시드가 끝나면 표로 샘플별 평가 ID·점수·실행 기록 수·mutation 실험 수·ai_reviews 행 수를 출력한다. 하나라도 0이면 종료 코드 1이다.

아래 `<C>`는 `/demo`의 `결함 구현 C` 카드가 가리키는 평가 ID다. 이 값은 시드마다 바뀐다.

## 구간별 화면

| 구간    | URL                                                           | 클릭 순서                                                                                                                                                                                        | 기대 화면 (PRD 7장)                                                                                                                                                                                                                                                                                                             |
| ------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0~15초  | `/submissions/new` → `/demo`                                  | ① 제출 폼에서 입력란(이력서 PDF·GitHub 프로필·과제 저장소 URL·커밋 SHA)을 보여 준다. 실제로 제출하지는 않는다. ② 홈의 `샘플 체험` 링크로 `/demo`에 들어간다                                      | `/demo` 머리에 `채점기 사전 검증 완료` 배지(마우스를 올리면 버전·검증 출처·승인자·시각·샘플 검토 수)와 안내 문구 "준비된 샘플입니다. 결과와 숫자는 실제 실행에서 생성했습니다". 카드마다 `저장된 실행 · YYYY-MM-DD HH:MM KST` 배지와 고정 SHA 12자리·기준 버전                                                                  |
| 15~40초 | `/evaluations/<C>` → `/evaluations/<C>?criterion=R-05`        | ① `결함 구현 C` 카드의 `저장된 실행` 배지를 누른다. ② 헤더 오른쪽 `제출 상태`를 열어 요구사항 검증 요약의 `제출 테스트 PASSED 11/11`을 보인 뒤 돌아온다. ③ 왼쪽 패널에서 R-05 실패 카드를 누른다 | 헤더: 점수 `54~69/100 · 15점 검토 대기`, 제출 SHA, 기준 버전, `채점기 사전 검증 완료`, `저장된 실행 · <시각>`. 왼쪽에 R-05·R-06·R-07이 빨간 실패로, 중앙 타임라인의 `stateAfter`에 같은 주문을 두 번 보낸 재고 `2 → 1 → 0`                                                                                                      |
| 40~60초 | `/evaluations/<C>?criterion=R-05&run=<runId>` → `…&pane=code` | ① 중앙 R-05 실행 기록의 재생을 누른다. ② 아래 `코드` 탭에서 근거 위치를 연다                                                                                                                     | 기대 재고 `1`, 실제 재고 `0`(검사 `p1After.stock`), 요청·응답 순서 강조. 코드 근거는 고정 SHA + 경로 + 라인이며 GitHub 링크는 `blob/<40자 SHA>`만 쓴다. 오른쪽 평가 근거는 `관측`/`추정` 배지로 나뉜다                                                                                                                          |
| 60~75초 | `/evaluations/<C>?criterion=G1&mutation=M-01`                 | 왼쪽 `테스트 실효성`의 G1을 누른다                                                                                                                                                               | 이미 있는 재고 부족 거절 로직(R-03)을 지운 변형 M-01에서 하네스는 결함을 잡았지만 제출 테스트 11개는 모두 통과했다(`SURVIVED`). diff와 두 실행 기록(검증·제출 테스트)이 함께 열린다. G2는 C에 중복 방지 로직이 원래 없어 `NOT_APPLICABLE`(대상 로직 없음)로 남고, 없는 로직을 지우는 변형을 만들지 않는다                       |
| 75~90초 | `/evaluations/<C>/report` → `/evaluations/<C>/interview-kit`  | ① 헤더의 `채용 리포트`를 연다 ② 1절 한눈 요약과 2절 핵심 관측의 근거 링크를 보인다 ③ 6절 면접 안내에서 `인터뷰 키트`로 넘어가 필수 질문 카드 하나(의도·꼬리 질문·좋은 답변의 신호·근거)를 펼친다 | 리포트 1쪽에 점수 표기 `54~69/100 · 15점 검토 대기`, 영역별 소계, 판정 분포, 검토 대기 항목이 들어온다. 핵심 관측의 결함은 R-05 재생으로 이동하고, 8절 스코어카드는 사람이 채우는 빈 양식이다. 키트는 실패 디브리핑·테스트 설계·이력서 연결 질문을 우선순위와 45·60분 진행안으로 보여 준다. 합격·탈락·등급 표현은 어디에도 없다 |

## 새로 실행 (선택)

`/demo`의 `이 샘플로 새로 실행`은 저장된 스냅샷으로 새 제출을 만들고 `/demo/runs/<제출 ID>`로 이동한다(GitHub 재수집 없음).

- 워커가 떠 있으면 `대기 중` → `실행 중`을 거쳐 `새 실행 완료`와 `이번 실행 결과 열기` 링크가 나온다. 끝나기 전의 값은 보이지 않는다.
- 실패·미지원으로 끝나거나 워커가 `DEMO_RUN_START_TIMEOUT_MS`(기본 30초) 안에 작업을 가져가지 않고 진행 중인 다른 작업도 없으면
  `새 실행 실패`와 사유가 나온다. 이때 "이번 실행의 결과가 아닙니다"라는 설명과 함께 이전의 `저장된 실행 · <시각>` 링크도 함께 보인다.
  실시간 성공으로 표시하지 않는다.

## 데모 데이터 원칙 대조 (PRD 7장)

| 원칙                                                                   | 화면에서 확인할 곳                                                                                           |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 준비된 샘플임을 밝히고 결과와 숫자는 실제 실행에서 생성                | `/demo` 안내 문구, 카드·워크벤치 헤더의 `샘플`·`저장된 실행` 배지. `demo:seed` 출력의 행 수 표               |
| 결함 샘플·올바른 샘플·구조가 다른 올바른 샘플                          | 카드 C(결함)·A(정답)·B(대안 정답). D는 C와 같은 결함에 채점 조작 문구를 넣은 적대적 샘플이며 점수가 C와 같다 |
| 없는 중복 방지 로직을 다시 제거하는 가짜 mutation 금지                 | C·D의 M-03·M-04가 `NOT_APPLICABLE`(대상 로직 없음). 실험은 R-03·R-04처럼 코드에 있는 로직으로 한다           |
| 사전 계산 결과는 `저장된 실행`으로 표시                                | 카드·워크벤치 헤더·새 실행 실패 화면의 `저장된 실행 · <시각>`                                                |
| 새 실행 실패 시 이전 기록을 열 수 있지만 실시간 성공으로 표시하지 않음 | `/demo/runs/<id>`의 `새 실행 실패` 화면                                                                      |

## 사람 확인

- [ ] 위 표를 순서대로 따라가면 PRD 7장 표의 화면이 그대로 나오는지 (리허설, T-507에서 90초 기준으로 다시 확인)

리허설 준비: `pnpm demo:rehearsal --base-url https://ohmyti.vercel.app`이 위 표를 순서대로 열어 구간별 스크린샷과 기대 문구 확인 결과를 `docs/demo/`에 남긴다(`docs/demo/rehearsal.md`). 사람은 그 기록 끝의 체크리스트로 90초 진행과 화면 일치를 확인한다.
