# 7단계 게이트: 페르소나 4종의 키트·리포트 회귀 (T-708)

`pnpm gate:personas`가 만든 기록이다. 배포 환경에 페르소나 4종을 제출해 실제 LLM으로 인터뷰 키트(T-702·T-704)와 채용 리포트(T-705·T-706)를 만들고, 키트의 질문 슬롯·우선순위·유형별 질문 수를 `samples/personas/expected-matrix.json`의 기대값과 대조한다. 질문은 모두 결정적 검사(`lintInterviewQuestion`)를 통과하고 저장된 근거를 하나 이상 참조해야 하며, 리포트의 판정·점수는 워크벤치(평가 조회 API)의 값과 같아야 하고 시스템이 쓴 문장에 금지 표현(PRD 14.4)이 없어야 한다. 기본 질문 대체와 버린 LLM 출력은 품질 관찰이라 경고로 남긴다.

이 파일은 게이트를 돌릴 때마다 덮어쓴다. 8단계(T-801·T-802) 개선의 전후 비교는 [`docs/gates/stage8.md`](stage8.md)에 있다.

- 결과: **통과**
- 배포: https://ohmyti.vercel.app
- 실행 일시: 2026-09-20T04:34:56.292Z ~ 2026-09-20T04:47:49.387Z
- rubric: `v2-be07fb44`

## 키트 생성 요약

| 페르소나 | 평가                                                                                     | 질문 수 | 기본 질문 대체 | LLM | 모델 · 프롬프트                            | 버린 항목 | 진행안(45·60분)               |
| -------- | ---------------------------------------------------------------------------------------- | ------: | -------------: | --- | ------------------------------------------ | --------- | ----------------------------- |
| seojin   | [`1bc69b13`](https://ohmyti.vercel.app/evaluations/1bc69b13-00c6-4283-aa1b-52e6f5616362) |       9 |            0/9 | OK  | deepseek-flash · interview-kit@v2+fcd7f772 | 없음      | 45분 45분·6문 / 60분 45분·6문 |
| taeyun   | [`f9d4d72a`](https://ohmyti.vercel.app/evaluations/f9d4d72a-72d0-4a2f-9907-11563827bcf4) |      11 |           0/11 | OK  | deepseek-flash · interview-kit@v2+fcd7f772 | 없음      | 45분 44분·5문 / 60분 55분·7문 |
| gaeun    | [`855fa55b`](https://ohmyti.vercel.app/evaluations/855fa55b-8530-4782-8c12-745fdaefd9f4) |      14 |           0/14 | OK  | deepseek-flash · interview-kit@v2+fcd7f772 | 없음      | 45분 44분·5문 / 60분 58분·7문 |
| dohyun   | [`8dacf67a`](https://ohmyti.vercel.app/evaluations/8dacf67a-16b6-4295-bc4b-d0d9a9e6ba10) |      11 |           0/11 | OK  | deepseek-flash · interview-kit@v2+fcd7f772 | 없음      | 45분 45분·6문 / 60분 51분·7문 |

## 채용 리포트

| 페르소나 | 점수 표시(워크벤치 = 리포트) | 판정 분포                       | 필수 질문 | 이력서 연결   | 설계 검토 | PDF 쪽 수 (리포트 / 키트) | 금지 표현 |
| -------- | ---------------------------- | ------------------------------- | --------: | ------------- | --------- | ------------------------- | --------- |
| seojin   | 90~100/100 · 10점 검토 대기  | PASS 14, INCONCLUSIVE 1         |         3 | AVAILABLE 5건 | AVAILABLE | 4 / 8                     | 0건       |
| taeyun   | 78~88/100 · 10점 검토 대기   | PASS 12, FAIL 2, INCONCLUSIVE 1 |         3 | AVAILABLE 5건 | AVAILABLE | 5 / 8                     | 0건       |
| gaeun    | 37~57/100 · 20점 검토 대기   | PASS 6, FAIL 6, INCONCLUSIVE 3  |         4 | AVAILABLE 3건 | AVAILABLE | 5 / 8                     | 0건       |
| dohyun   | 75~85/100 · 10점 검토 대기   | PASS 11, FAIL 3, INCONCLUSIVE 1 |         3 | AVAILABLE 4건 | AVAILABLE | 4 / 8                     | 0건       |

### 화면 캡처 (채용 리포트 · 인터뷰 키트)

| 화면                 | 뷰포트  | HTTP | 가로 넘침 | 콘솔 오류 | 파일                                                 | 문제 |
| -------------------- | ------- | ---: | --------: | --------: | ---------------------------------------------------- | ---- |
| report-seojin        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-seojin-desktop.png`        | 없음 |
| interview-kit-seojin | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-seojin-desktop.png` | 없음 |
| report-seojin        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-seojin-mobile.png`         | 없음 |
| interview-kit-seojin | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-seojin-mobile.png`  | 없음 |
| report-taeyun        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-taeyun-desktop.png`        | 없음 |
| interview-kit-taeyun | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-taeyun-desktop.png` | 없음 |
| report-taeyun        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-taeyun-mobile.png`         | 없음 |
| interview-kit-taeyun | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-taeyun-mobile.png`  | 없음 |
| report-gaeun         | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-gaeun-desktop.png`         | 없음 |
| interview-kit-gaeun  | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-gaeun-desktop.png`  | 없음 |
| report-gaeun         | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-gaeun-mobile.png`          | 없음 |
| interview-kit-gaeun  | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-gaeun-mobile.png`   | 없음 |
| report-dohyun        | desktop |  200 |       0px |         0 | `docs/gates/stage7/report-dohyun-desktop.png`        | 없음 |
| interview-kit-dohyun | desktop |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-dohyun-desktop.png` | 없음 |
| report-dohyun        | mobile  |  200 |       0px |         0 | `docs/gates/stage7/report-dohyun-mobile.png`         | 없음 |
| interview-kit-dohyun | mobile  |  200 |       0px |         0 | `docs/gates/stage7/interview-kit-dohyun-mobile.png`  | 없음 |

## 페르소나별 키트 전문

### seojin · seojin-order-api

#### Q1. R-03 재고 부족 거절이 기대대로 동작했습니다. 재고와 요청 수량을 비교하는 판단을 코드의 어느 지점에 두셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 통과한 재고 검증 동작을 스스로 설명할 수 있는지, 판단 위치를 짚을 수 있는지 확인한다.
- 꼬리 질문: 재고 확인과 차감 사이의 순서는 어떻게 되나요? / 재고가 0인 상품과 수량이 재고를 넘는 경우를 같은 경로로 처리하셨나요? / 이 판단을 다른 계층으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 재고 확인과 차감의 순서를 코드 위치와 함께 설명한다. / 재고 0과 초과 요청을 같은 조건으로 처리하는지 구분해 말한다.
- 우려 신호: 통과 결과만 말하고 판단 위치를 짚지 못한다. / 재고 확인 시점을 상황에 따라 다르게 말한다.
- 근거: 기준 R-03, 실행 기록 ff2fa0ae · R-03, 코드 src/http/routes.ts:46-67, 코드 src/http/routes.ts:40-44

#### Q2. R-04 입력 검증이 기대대로 동작했습니다. quantity와 productId를 검사하는 규칙을 어디에 모아 두셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 검증 규칙의 위치와 적용 범위를 스스로 설명하는지 확인한다.
- 꼬리 질문: 검증 실패로 끝난 요청의 키를 기록에 남기지 않으려면 어떤 조건이 필요했나요? / JSON이 아닌 본문은 어느 단계에서 걸러지나요? / 검증 규칙을 하나 더 추가한다면 어느 파일을 고치시겠어요?
- 좋은 답변의 신호: 검증 규칙의 위치와 실패 시 기록 처리 조건을 함께 설명한다. / 본문 파싱 실패와 필드 검증 실패를 구분해 말한다.
- 우려 신호: 검증 항목을 나열만 하고 위치를 말하지 않는다. / 실패한 요청의 키가 기록에 남는지 여부를 모른다.
- 근거: 기준 R-04, 실행 기록 a16a7904 · R-04, 코드 src/http/routes.ts:46-67, 코드 src/http/routes.ts:40-44

#### Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어오는 상황을 가정하겠습니다. 지금 구조에서 가장 먼저 문제가 드러날 지점은 어디인가요? <sub>요구사항 확장 · MUST · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인스턴스가 늘어날 때 현재 구조의 한계를 어디로 짚는지 확인한다.
- 꼬리 질문: 그 지점을 먼저 고르신 이유는 무엇인가요? / 인스턴스 간에 무엇을 공유해야 같은 키 요청이 한 번만 처리되나요? / 공유 저장소를 두면 응답 시간과 실패 처리에서 무엇이 달라지나요?
- 좋은 답변의 신호: 인메모리 상태가 인스턴스마다 갈라지는 지점을 구체적으로 짚는다. / 멱등성 기록과 재고를 어디에 두어야 하는지 구분해 설명한다.
- 우려 신호: 인스턴스를 늘리면 된다는 수준에서 멈춘다. / 공유 저장소 도입 시 생기는 실패 상황을 말하지 않는다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q4. registerRoutes가 51줄로 가장 긴 함수입니다. 이 함수 안에서 라우팅과 도메인 규칙이 어떻게 나뉘어 있는지 설명해 주시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 가장 긴 함수의 책임 구성을 스스로 설명하는지 확인한다.
- 꼬리 질문: 이 함수에서 HTTP 요청·응답 객체를 직접 다루는 부분은 어디까지인가요? / 재고 차감이나 멱등성 판단이 이 함수 밖에 있다면 어느 모듈인가요? / 이 함수를 나눈다면 어떤 경계를 먼저 그으시겠어요?
- 좋은 답변의 신호: 라우팅과 도메인 규칙의 경계를 코드 위치로 구분해 말한다. / 도메인 규칙이 요청·응답 객체에 의존하는지 여부를 구체적으로 답한다.
- 우려 신호: 함수 길이를 문제로 인식하지 않는다. / 경계를 말하면서 실제 코드 위치를 짚지 못한다.
- 근거: 코드 src/http/routes.ts:30-80, 기준 R-12

#### Q5. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정하겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 저장 방식이 바뀔 때 구조를 어디부터 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 저장 위치를 바꾸면 기존 동작 중 무엇을 다시 확인하시겠어요? / 초기화 경로는 저장 방식이 바뀌면 어떻게 달라져야 하나요?
- 좋은 답변의 신호: 바꿀 범위를 코드의 구체적인 위치로 말한다. / 저장 위치를 옮길 때 새로 생기는 실패 상황을 든다.
- 우려 신호: 도구 이름만 대고 코드에서 바꿀 부분을 말하지 않는다. / 초기화 경로의 변화를 고려하지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q6. 재고 선점 시스템에서 낙관적 잠금 충돌이 났을 때 재시도는 어떤 조건으로 몇 번까지 수행하도록 설계하셨나요? <sub>이력서 연결 · SHOULD · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서의 낙관적 잠금·재시도 설계가 실제로 어떤 충돌 조건과 한도를 다뤘는지 확인한다.
- 꼬리 질문: 버전 비교는 어느 저장소 계층에서 수행되나요? / 재시도 사이에 지연이나 백오프를 두었나요? / TTL 만료로 회수된 예약과 충돌한 요청은 어떻게 처리되나요?
- 좋은 답변의 신호: 충돌 감지 지점과 재시도 횟수·백오프를 구체적으로 설명한다 / TTL 만료 회수와 재시도가 겹치는 경우의 처리 순서를 설명한다
- 우려 신호: 재시도를 '충돌하면 다시 시도한다' 수준으로만 설명한다 / 재시도 상한이나 실패 시 응답을 설명하지 않는다
- 근거: 맥락 연결 cd8cb39b, 기준 R-08

#### Q7. R-12 설계 항목을 보겠습니다. 새 오류 코드를 하나 추가한다면 어느 위치를 고치게 되는지 설명해 주시겠어요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:R-12`
- 의도: 오류 코드와 상태 코드 매핑이 한 곳에 모여 있는지 확인한다.
- 꼬리 질문: 상태 코드와 오류 코드의 매핑은 어디에 정의되어 있나요? / 멱등성 기록의 저장과 조회는 어떤 경계 뒤에 있나요? / 저장 구현을 바꿀 때 주문 생성 로직에서 고칠 부분이 있나요?
- 좋은 답변의 신호: 오류 코드 추가 시 수정할 위치를 구체적으로 짚는다. / 멱등성 저장소의 경계와 교체 비용을 함께 설명한다.
- 우려 신호: 매핑이 여러 곳에 흩어져 있음을 인지하지 못한다. / 저장소 교체 시 주문 생성 로직 수정 여부를 판단하지 못한다.
- 근거: 기준 R-12

#### Q8. 결제·주문 생성 API에 붙인 멱등성 키 미들웨어에서 같은 키로 진행 중인 요청이 두 번 들어오면 어떻게 병합하도록 구현하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서의 멱등성 미들웨어가 동시 요청 병합을 어떤 방식으로 보장했는지 확인한다.
- 꼬리 질문: 진행 중 요청의 상태는 어디에 저장했나요? / 첫 요청이 실패하면 대기 중이던 요청은 어떤 응답을 받나요? / 같은 키에 다른 본문이 오면 어떤 기준으로 충돌을 판정하나요?
- 좋은 답변의 신호: in-flight 상태 저장 위치와 병합 시점을 구체적으로 설명한다 / 첫 요청 실패 시 대기 요청의 응답 정책을 설명한다
- 우려 신호: 병합을 '락을 건다' 수준으로만 설명한다 / 키 충돌 판정 기준(메서드·경로·본문 해시)을 언급하지 않는다
- 근거: 맥락 연결 5147a131, 기준 R-05

#### Q9. 중복 주문으로 인한 결제 취소·환불 건수 0건은 어떤 지표와 기간으로 집계하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · OPERABILITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 수치 주장의 측정 방식과 집계 범위를 확인한다.
- 꼬리 질문: 환불 건수는 어떤 로그나 대시보드에서 집계했나요? / 멱등성 키 도입 전후 비교 기준 기간은 어떻게 잡았나요? / 키가 없는 클라이언트 요청도 집계에 포함되었나요?
- 좋은 답변의 신호: 집계 대상 지표와 데이터 출처를 구체적으로 설명한다 / 도입 전후 비교 기간과 조건을 설명한다
- 우려 신호: 수치의 출처를 설명하지 못한다 / 측정 기준 없이 결과만 반복한다
- 근거: 맥락 연결 9852b802, 기준 R-06

### taeyun · taeyun-order-api

#### Q1. R-06 실행 기록에서 같은 키로 다른 본문을 보냈을 때 201이 돌아왔습니다. 요청이 들어온 뒤 저장된 기록과 본문을 비교하는 지점을 코드 어디에 두셨나요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-06`
- 의도: 멱등 키 충돌을 걸러내는 판단이 코드 어디에서 일어나는지 지원자가 스스로 짚는지 확인한다.
- 꼬리 질문: 저장된 기록과 새 본문이 다르다는 것을 어떤 값으로 판단하시나요? / 그 비교가 실행되지 않는 경로는 무엇인가요? / 비교 시점을 저장 전후 어디에 둘지 정할 때 어떤 기준을 쓰시겠어요?
- 좋은 답변의 신호: 멱등 기록 조회와 본문 비교가 일어나는 위치를 코드에서 짚는다. / 충돌 판정과 새 주문 생성이 갈라지는 조건을 구분해 설명한다.
- 우려 신호: 201이 나온 이유를 도구나 하네스 문제로 돌린다. / 기록 조회와 본문 비교 중 어느 쪽이 빠졌는지 말하지 못한다.
- 근거: 기준 R-06, 실행 기록 8cdc4c86 · R-06, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23, 코드 src/routes/order-routes.ts:30-33

#### Q2. R-07 실행 기록에서 같은 키의 동시 요청 10건 중 5건만 201이고 응답 id가 서로 달랐습니다. 같은 키의 요청이 동시에 들어올 때 주문 생성이 어떤 순서로 진행되는지 설명해 주시겠어요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-07`
- 의도: 동시 요청에서 멱등성 기록과 재고 차감이 어떤 순서로 일어나는지 지원자가 설명하는지 확인한다.
- 꼬리 질문: 기록을 확인하는 시점과 기록을 남기는 시점 사이에 다른 요청이 끼어들면 어떤 일이 생기나요? / 재고가 0이 된 결과는 어느 단계에서 발생했다고 보시나요? / 같은 키의 요청을 한 번만 처리하도록 만들려면 어느 지점을 먼저 바꾸시겠어요?
- 좋은 답변의 신호: 기록 확인과 기록 저장 사이의 간격을 문제 지점으로 짚는다. / 동시 요청에서 재고 차감이 여러 번 일어나는 경로를 설명한다.
- 우려 신호: 동시성 문제를 언급하지 않고 순차 호출만 가정한다. / 재고가 0이 된 이유를 설명하지 못한다.
- 근거: 기준 R-07, 실행 기록 8ea0a2f4 · R-07, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q3. R-03은 재고 부족 요청을 409로 거절하고 재고를 그대로 두었습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 통과한 거절 동작의 판단 위치와 기준을 지원자가 스스로 설명하는지 확인한다.
- 꼬리 질문: 재고와 같은 수량의 요청을 허용으로 본 기준은 무엇인가요? / 거절로 끝난 요청이 재고를 건드리지 않도록 어떤 순서를 쓰셨나요? / 이 판단을 다른 계층으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 비교 조건과 차감 순서를 구분해 설명한다. / 재고와 같은 수량을 허용한 기준을 수치로 말한다.
- 우려 신호: 통과 결과만 말하고 판단 위치를 짚지 못한다. / 거절과 차감의 순서를 뒤바꿔 설명한다.
- 근거: 기준 R-03, 실행 기록 a9f2d522 · R-03, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q4. 가장 긴 함수가 라우팅 파일의 createOrderRoutes로 34줄입니다. 이 함수 안에서 HTTP 처리와 도메인 규칙이 어떻게 나뉘어 있는지 설명해 주시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 라우팅 함수 안에서 HTTP 처리와 도메인 규칙의 경계를 지원자가 어떻게 인식하는지 확인한다.
- 꼬리 질문: 이 함수에서 요청·응답 객체에 직접 닿는 부분은 어디까지인가요? / 재고 차감과 멱등성 판단은 어느 모듈에 있나요? / 이 함수를 나눈다면 어떤 경계를 먼저 그으시겠어요?
- 좋은 답변의 신호: 함수 안에서 HTTP 의존 부분과 규칙 부분을 구분해 짚는다. / 도메인 규칙이 요청·응답 객체에 닿는지 여부를 스스로 판단한다.
- 우려 신호: 함수 길이를 문제로 보지 않고 그대로 두겠다고만 말한다. / HTTP 처리와 도메인 규칙을 같은 것으로 본다.
- 근거: 코드 src/routes/order-routes.ts:8-41, 기준 R-12

#### Q5. R-04는 여러 잘못된 입력을 400으로 거절하고 재고를 그대로 두었습니다. 입력 검증을 통과한 요청만 멱등성 기록에 남도록 어떤 순서를 쓰셨나요? <sub>강점 확인 · SHOULD · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 검증과 멱등성 기록의 순서를 지원자가 설명하는지 확인한다.
- 꼬리 질문: 검증 실패로 끝난 요청의 키가 기록에 남지 않는 이유는 무엇인가요? / 검증 항목을 추가할 때 수정할 위치는 어디인가요? / 검증을 도메인 규칙 안으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 검증과 기록 저장의 순서를 단계로 나눠 설명한다. / 검증 실패 시 재고가 변하지 않는 이유를 순서로 설명한다.
- 우려 신호: 검증과 기록의 순서를 구분하지 않는다. / 검증 항목이 어디에 모여 있는지 짚지 못한다.
- 근거: 기준 R-04, 실행 기록 4c931fe0 · R-04, 코드 src/routes/order-routes.ts:25-28, 코드 src/routes/order-routes.ts:20-23

#### Q6. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인스턴스가 늘어날 때 어떤 상태 공유 지점을 먼저 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 인스턴스마다 상태가 따로 있으면 같은 키의 요청은 어떻게 되나요? / 공유 저장소를 두는 대신 다른 방법을 쓴다면 무엇을 고려하시겠어요?
- 좋은 답변의 신호: 인스턴스 간에 공유되지 않는 상태를 구체적으로 짚는다. / 같은 키의 요청이 다른 인스턴스로 갈 때 생기는 문제를 설명한다.
- 우려 신호: 인스턴스를 늘리는 것만 언급하고 상태 공유를 말하지 않는다. / 도구 이름만 대고 바꿀 지점을 짚지 못한다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q7. 결제 웹훅 멱등 처리에서 같은 키로 다른 내용이 재전송되면 어떻게 처리하도록 설계하셨나요? <sub>이력서 연결 · SHOULD · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적힌 웹훅 멱등 처리의 충돌·동시성 조건까지의 보장 범위를 확인한다.
- 꼬리 질문: 그 서비스에서 멱등 키는 어떤 값으로 만드셨나요? / 같은 키의 요청이 동시에 두 번 들어오면 어느 단계에서 하나로 정리되나요? / 이번 과제의 인메모리 구현과 비교하면 저장 위치와 트랜잭션 경계에서 무엇이 달라지나요?
- 좋은 답변의 신호: 키 저장 위치와 만료, 본문 비교 기준을 구체적으로 설명한다 / 동시 요청을 막는 지점(유일 제약·잠금)과 재시도 조건을 구분해 설명한다
- 우려 신호: 멱등성을 '같은 요청은 한 번만 처리한다'는 일반론으로만 설명한다 / 같은 키에 다른 본문이 오는 경우를 다루지 않는다
- 근거: 맥락 연결 8bfcb46b, 기준 R-05

#### Q8. R-12 설계 항목에서 멱등성 기록의 저장과 조회가 어디에 있는지 보겠습니다. 인메모리 구현을 다른 저장소로 바꿀 때 주문 생성 로직에서 고치지 않아도 되는 경계를 어떻게 그으셨나요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:R-12`
- 의도: 멱등성 저장소를 교체할 때의 경계가 코드에서 어떻게 잡혀 있는지 확인한다.
- 꼬리 질문: 저장소를 바꿀 때 주문 생성 로직이 의존하는 것은 무엇인가요? / 새 오류 코드를 추가할 때 수정할 위치는 몇 곳인가요? / 상태 코드와 오류 코드 매핑은 어디에 모여 있나요?
- 좋은 답변의 신호: 저장소 교체 시 영향 범위를 코드 위치로 말한다. / 오류 코드 매핑이 모인 위치를 짚는다.
- 우려 신호: 계층 분리를 일반론으로만 설명한다. / 저장소 교체 시 고칠 위치를 짚지 못한다.
- 근거: 기준 R-12

#### Q9. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · OPTIONAL · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 영속성이 필요해질 때 어떤 상태 저장 지점을 먼저 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 저장 위치를 바꾸면 기존 동작 중 무엇을 다시 확인하시겠어요? / 초기화 경로는 새 저장 구조에서 어떻게 동작해야 한다고 보시나요?
- 좋은 답변의 신호: 메모리에만 있는 상태를 구체적인 위치로 짚는다. / 저장 위치를 바꾼 뒤 재확인할 동작을 든다.
- 우려 신호: 도구 이름만 대고 바꿀 지점을 짚지 못한다. / 초기화 경로와의 관계를 말하지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q10. 회사 예약 API에서 중복 예약 검증은 어느 계층에서 어떤 조건으로 수행하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서의 중복 예약 검증이 어느 계층에 위치하고 어떤 경계 조건을 다루는지 확인한다.
- 꼬리 질문: 겹침 판정은 시작·종료 시각을 어떤 비교식으로 처리하나요? / 검증과 저장 사이에 다른 요청이 끼어들면 어떻게 되나요? / 개인 프로젝트의 인메모리 저장 방식과 비교하면 무엇을 포기하게 되나요?
- 좋은 답변의 신호: 겹침 조건의 경계(끝과 시작이 같은 경우)를 명시해 설명한다 / 검증 위치와 저장 시점의 순서를 근거와 함께 설명한다
- 우려 신호: 겹침 판정을 '시간이 겹치면 거절'로만 설명한다 / 검증과 저장 사이의 경합 가능성을 언급하지 않는다
- 근거: 맥락 연결 70c6e48e, 기준 R-08

#### Q11. 계층을 분리한 뒤 신규 기능을 추가할 때 변경 범위가 실제로 어떻게 달라졌나요? <sub>이력서 연결 · OPTIONAL · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 이력서의 계층 분리 리팩터링이 변경 범위에 미친 영향을 구체적 사례로 확인한다.
- 꼬리 질문: 그때 분리 기준은 무엇이었나요? / 계층 간 의존 방향을 어떻게 강제하셨나요? / 분리를 하지 않았을 때와 비교해 어떤 비용이 생겼나요?
- 좋은 답변의 신호: 특정 기능 추가 시 수정한 파일과 계층을 구체적으로 제시한다 / 의존 방향과 테스트 대체 지점을 함께 설명한다
- 우려 신호: '계층을 나눴다'는 수준에서 그친다 / 변경 범위가 줄었다는 근거를 제시하지 못한다
- 근거: 맥락 연결 d6b041a6, 기준 R-12

### gaeun · gaeun-order-api

#### Q1. R-05 재전송 케이스에서 두 번째 호출이 새 주문을 만들었고 재고도 두 번 줄었습니다. 같은 키의 두 번째 요청을 처리하는 흐름을 코드에서 어떻게 따라가 보시겠어요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-05`
- 의도: 멱등 재전송이 깨진 원인을 코드 흐름에서 스스로 좁혀 가는지 확인한다.
- 꼬리 질문: 첫 번째 요청의 결과를 어디에 남기고 있나요? / 두 번째 요청이 들어올 때 그 기록을 어떤 조건으로 찾나요? / 기록을 남기는 시점을 응답 직전으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 키 조회와 주문 생성 순서를 구분해 설명한다. / 재고 차감이 두 번 일어난 경로를 코드 위치로 짚는다.
- 우려 신호: 재고만 문제라고 말하고 키 처리 흐름을 언급하지 않는다. / 기록 저장 시점을 말하지 못한다.
- 근거: 기준 R-05, 실행 기록 e639f2e2 · R-05, 코드 src/app.ts:23-31, 코드 src/app.ts:43-69, 코드 src/app.ts:33-41

#### Q2. R-06 충돌 케이스에서 다른 본문의 재요청이 201로 통과했습니다. 같은 키로 들어온 요청의 본문이 이전과 같은지 판단하는 부분을 어디에 두셨나요? <sub>실패 디브리핑 · MUST · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-06`
- 의도: 멱등 키 충돌을 구분하는 판단 위치와 기준을 확인한다.
- 꼬리 질문: 본문 비교에서 무엇을 같은 요청으로 보시나요? / 비교 결과가 다를 때 어떤 응답을 돌려주시나요? / 이 판단을 재고 검사보다 앞에 두면 무엇이 달라지나요?
- 좋은 답변의 신호: 키 조회와 본문 비교를 한 흐름으로 설명한다. / 충돌 응답과 재고 부족 응답을 구분해 말한다.
- 우려 신호: 충돌과 재고 부족을 같은 오류로 처리한다고 말한다. / 본문 비교 기준을 말하지 못한다.
- 근거: 기준 R-06, 실행 기록 22bd2961 · R-06, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:33-41

#### Q3. G1 그룹에서 재고 부족 검사를 없앤 변형이 제출 테스트를 통과했습니다. 재고 부족 상황을 검증하는 테스트를 설계할 때 어떤 입력을 고르셨나요? <sub>테스트 설계 · MUST · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G1`
- 의도: 경계·검증 변이를 잡는 테스트 입력을 고르는 기준이 있는지 확인한다.
- 꼬리 질문: 재고와 같은 수량을 쓰는 경우를 테스트에 넣으셨나요? / 이 변형이 어떤 단언에서 걸렸어야 한다고 보시나요? / 수량 경계값을 고를 때 어떤 기준을 쓰시나요?
- 좋은 답변의 신호: 재고 초과와 재고 동일 수량을 구분해 테스트 입력으로 든다. / 살아남은 변형을 잡을 단언을 구체적으로 말한다.
- 우려 신호: 테스트를 더 쓰겠다고만 말하고 입력을 정하지 않는다. / 변이가 살아남은 이유를 도구 문제로 돌린다.
- 근거: 기준 G1, 변이 M-01, 코드 src/app.ts:61-61

#### Q4. R-03은 재고 부족 요청을 409로 거절하고 재고를 그대로 두었습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 통과한 거절 동작의 판단 위치와 순서를 스스로 설명하는지 확인한다.
- 꼬리 질문: 비교와 차감 중 어느 것을 먼저 하나요? / 재고가 0인 상품은 이 흐름에서 어떻게 처리되나요? / 이 판단을 다른 계층으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 비교와 차감의 순서를 구분해 설명한다. / 재고 0과 재고 초과를 같은 경로로 설명한다.
- 우려 신호: 통과했다는 결과만 말하고 판단 위치를 말하지 못한다. / 거절 시 재고를 건드리지 않는 이유를 설명하지 못한다.
- 근거: 기준 R-03, 실행 기록 3b457efa · R-03, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31

#### Q5. R-09 취소 케이스에서 취소 후 재고가 시드 값으로 돌아오지 않았습니다. 취소 요청을 처리하는 흐름에서 재고를 되돌리는 부분을 어떻게 찾아가시겠어요? <sub>실패 디브리핑 · SHOULD · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-09`
- 의도: 취소 시 재고 복구가 빠진 원인을 코드에서 좁혀 가는지 확인한다.
- 꼬리 질문: 취소 상태를 바꾸는 시점과 재고를 되돌리는 시점이 어떻게 다른가요? / 이미 취소된 주문의 재취소는 어디에서 걸러지나요? / 복구를 두 번 하지 않도록 무엇을 확인하시겠어요?
- 좋은 답변의 신호: 상태 변경과 재고 복구를 한 흐름으로 설명한다. / 재취소 차단 조건을 코드 위치로 짚는다.
- 우려 신호: 재고 복구 코드가 없다는 사실을 확인하지 않는다. / 재취소 시 복구가 두 번 일어나는 조건을 말하지 못한다.
- 근거: 기준 R-09, 실행 기록 e5399a97 · R-09, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:71-87, 코드 src/app.ts:33-41

#### Q6. 가장 긴 함수가 POST /orders 처리 한 곳에 모여 있습니다. 이 함수 안에서 요청 파싱과 재고·멱등성 규칙을 나눌 때 어떤 경계를 기준으로 삼으셨나요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 긴 함수 안에서 책임을 나누는 기준이 있는지 확인한다.
- 꼬리 질문: 이 함수에서 변경이 가장 잦은 부분은 어디인가요? / 규칙을 별도 모듈로 옮긴다면 어느 것부터 꺼내시겠어요? / 그 경계를 옮길 때 감수해야 하는 비용은 무엇인가요?
- 좋은 답변의 신호: HTTP 처리와 도메인 규칙을 구분해 설명한다. / 옮길 때 생기는 비용을 구체적으로 든다.
- 우려 신호: 계층 분리를 일반론으로만 설명한다. / 현재 함수의 어느 부분을 말하는지 짚지 않는다.
- 근거: 코드 src/app.ts:43-69, 기준 R-12

#### Q7. R-08은 서로 다른 키의 동시 요청에서 201이 5건, 409가 5건으로 나왔습니다. 재고를 확인하고 줄이는 사이에 다른 요청이 끼어들지 않게 어떻게 하셨나요? <sub>강점 확인 · SHOULD · 6분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-08`
- 의도: 통과한 동시성 동작의 정합성 유지 방식을 스스로 설명하는지 확인한다.
- 꼬리 질문: 재고 확인과 차감 사이에 다른 요청이 끼어들면 어떤 값이 관측되나요? / 이 처리가 비동기 경계를 넘는 지점이 있나요? / 요청 수가 더 늘어나면 이 방식에서 무엇이 먼저 한계에 닿나요?
- 좋은 답변의 신호: 확인과 차감이 끼어들 수 없는 단위임을 설명한다. / 동시 요청에서 재고가 음수가 되지 않는 조건을 든다.
- 우려 신호: 결과 수치만 말하고 정합성 유지 방식을 설명하지 못한다. / 비동기 경계에서의 위험을 인식하지 못한다.
- 근거: 기준 R-08, 실행 기록 e56bf1b0 · R-08, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31, 코드 src/app.ts:33-41

#### Q8. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인스턴스가 늘어날 때 구조를 어디부터 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 인스턴스 간에 공유되지 않는 상태는 무엇인가요? / 공유 저장소를 두면 기존 동작 중 무엇을 다시 확인하시겠어요?
- 좋은 답변의 신호: 인스턴스 간에 공유되지 않는 상태를 구체적으로 짚는다. / 바꿀 범위를 코드 위치로 말한다.
- 우려 신호: 도구 이름만 대고 코드에서 바꿀 부분을 말하지 않는다. / 멱등성 기록의 공유 필요성을 언급하지 않는다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q9. 팀 프로젝트에서 멱등성은 어떤 기준으로 같은 요청인지 판단하도록 설계하셨나요? <sub>이력서 연결 · SHOULD · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적은 멱등성 설계 경험이 키와 요청 본문을 어떻게 비교하는 수준이었는지 확인한다.
- 꼬리 질문: 멱등 키는 어떤 값으로 만드셨나요? / 같은 키로 본문이 다른 요청이 오면 어떤 응답을 주도록 정했나요? / 동시에 같은 키가 두 번 들어올 때 어느 지점에서 하나로 정리되나요?
- 좋은 답변의 신호: 키 저장 위치와 만료, 본문 비교 기준을 구체적으로 설명한다 / 동시 요청을 막는 지점과 재시도 조건을 구분해 설명한다
- 우려 신호: 멱등성을 '같은 요청은 한 번만 처리한다'는 일반론으로만 설명한다 / 본문이 다른 재요청의 처리 기준을 설명하지 않는다
- 근거: 맥락 연결 f0e84a96, 기준 R-05

#### Q10. R-04 검증 케이스에서 수량 누락과 0, 음수가 모두 201로 통과했습니다. 요청 본문을 검사하는 부분을 코드에서 어떻게 찾아가시겠어요? <sub>실패 디브리핑 · OPTIONAL · 8분 · DEBUGGING · LLM</sub>

- 슬롯: `FAILURE_DEBRIEF:R-04`
- 의도: 입력 검증이 빠진 원인을 코드에서 좁혀 가는지 확인한다.
- 꼬리 질문: 수량이 어떤 타입이어야 하는지 어디에서 정하나요? / 검사에서 걸린 요청의 키는 이후 어떻게 처리되나요? / 검증을 재고 확인보다 앞에 두면 무엇이 달라지나요?
- 좋은 답변의 신호: 검증 항목과 순서를 구분해 설명한다. / 400으로 끝난 요청이 멱등성 기록에 남지 않아야 하는 이유를 말한다.
- 우려 신호: 검증 코드가 없다는 사실을 확인하지 않는다. / 검증 실패 요청이 기록에 남는 문제를 인식하지 못한다.
- 근거: 기준 R-04, 실행 기록 9679beaf · R-04, 코드 src/app.ts:43-69, 코드 src/app.ts:23-31

#### Q11. 코드에 명시적 any가 세 곳 있습니다. 이 자리에서 타입을 좁히지 않고 any를 쓰기로 한 기준은 무엇이었나요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:explicit-any`
- 의도: 타입을 느슨하게 둔 판단의 기준과 영향 범위를 확인한다.
- 꼬리 질문: 이 값들에 어떤 타입이 들어올 수 있나요? / 타입을 좁히면 어느 검증이 대신하게 되나요? / 이 자리를 그대로 두면 이후 변경에서 무엇이 어려워지나요?
- 좋은 답변의 신호: any가 놓인 자리의 값 흐름을 설명한다. / 타입을 좁힐 때 대체할 검증을 구체적으로 든다.
- 우려 신호: any를 습관적으로 썼다고만 말한다. / 해당 위치의 값이 무엇인지 설명하지 못한다.
- 근거: 코드 src/app.ts:44-44, 코드 src/app.ts:90-90, 코드 src/data.ts:42-42, 기준 R-12

#### Q12. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · OPTIONAL · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 저장 요구가 바뀔 때 구조를 어디부터 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 저장 위치를 바꾸면 기존 동작 중 무엇을 다시 확인하시겠어요? / 초기화 경로는 이 변경과 어떻게 맞물리나요?
- 좋은 답변의 신호: 바꿀 범위를 코드의 구체적인 위치로 말한다. / 저장 위치를 옮길 때 새로 생기는 실패 상황을 든다.
- 우려 신호: 도구 이름만 대고 코드에서 바꿀 부분을 말하지 않는다. / 초기화 경로와의 관계를 언급하지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q13. 북마크 API에서 입력값 검증은 어떤 기준으로 어디에 두셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · REQUIREMENTS · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 이력서에 적은 검증 경험이 어떤 입력 조건까지 다뤘는지 확인한다.
- 꼬리 질문: 검증 실패 시 응답 형식은 어떻게 정하셨나요? / 검증 로직을 라우터와 저장소 중 어디에 두었나요? / 숫자 필드의 경계값은 어떻게 정하셨나요?
- 좋은 답변의 신호: 검증 위치와 실패 응답 형식을 구체적으로 설명한다 / 경계값과 누락 입력을 구분해 설명한다
- 우려 신호: 검증을 '필수값 체크' 수준으로만 설명한다 / 실패 응답의 상태 코드와 본문 형식을 설명하지 않는다
- 근거: 맥락 연결 d6c91840, 기준 R-04

#### Q14. jQuery 기반 화면을 React 컴포넌트로 전환할 때 상태 관리는 어떻게 옮기셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 이력서에 적은 전환 경험이 상태와 부수 효과를 어떻게 다뤘는지 확인한다.
- 꼬리 질문: 전환 전후로 상태를 누가 소유하는지 어떻게 달라졌나요? / DOM을 직접 조작하던 코드는 어떤 방식으로 바꾸셨나요? / 전환 과정에서 기존 동작이 깨진 사례가 있었나요?
- 좋은 답변의 신호: 상태 소유 위치와 부수 효과 처리 방식을 구체적으로 설명한다 / 전환 중 깨진 동작과 대응을 설명한다
- 우려 신호: 전환을 '컴포넌트로 나눴다' 수준으로만 설명한다 / 상태 관리 변경을 설명하지 않는다
- 근거: 맥락 연결 1f6941ab

### dohyun · dohyun-order-api

#### Q1. R-03은 재고보다 많은 수량과 재고 0인 상품 요청을 모두 거절했습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-03`
- 의도: 통과한 거절 동작의 판단 위치와 경계 처리를 스스로 설명하는지 확인한다.
- 꼬리 질문: 재고와 같은 수량을 허용하기로 한 기준은 무엇인가요? / 거절한 요청이 재고 값을 바꾸지 않았다는 것을 어디서 확인하셨나요? / 이 판단을 다른 계층으로 옮기면 무엇이 달라지나요?
- 좋은 답변의 신호: 비교 시점과 차감 시점의 순서를 구분해 설명한다. / 재고와 같은 수량이 통과하는 이유를 경계 기준으로 말한다.
- 우려 신호: 거절 결과만 말하고 판단 위치를 짚지 못한다. / 재고 확인과 차감을 같은 시점으로 뭉뚱그려 설명한다.
- 근거: 기준 R-03, 실행 기록 0a103a40 · R-03, 코드 src/index.ts:101-233, 코드 src/index.ts:78-92

#### Q2. R-04는 여러 잘못된 입력을 400으로 거절했습니다. 수량과 상품 식별자 검증을 어느 지점에서 하기로 정하셨나요? <sub>강점 확인 · MUST · 6분 · ROBUSTNESS · LLM</sub>

- 슬롯: `STRENGTH_DEPTH:R-04`
- 의도: 입력 검증의 위치와 순서를 스스로 설명하는지 확인한다.
- 꼬리 질문: 수량 0과 음수를 같은 규칙으로 처리한 기준은 무엇인가요? / 400으로 끝난 요청의 키를 기록에 남기지 않은 이유는 무엇인가요? / 검증 규칙을 추가할 때 어느 파일을 고치시겠어요?
- 좋은 답변의 신호: 검증 순서와 재고 확인의 선후를 구분해 설명한다. / 거절된 요청이 상태를 바꾸지 않는 이유를 저장 시점으로 설명한다.
- 우려 신호: 검증 항목을 나열만 하고 위치를 말하지 않는다. / 거절된 요청의 키가 기록에 남는지 여부를 모른다.
- 근거: 기준 R-04, 실행 기록 f1bb3993 · R-04, 코드 src/index.ts:101-233, 코드 src/index.ts:78-92

#### Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · MUST · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:multi-instance`
- 의도: 인스턴스가 늘어날 때 구조를 어디부터 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 인스턴스가 둘일 때 같은 키의 두 요청이 각각 들어가면 지금 구조는 어떻게 동작하나요? / 공유 저장소를 두면 새로 생기는 실패 상황은 무엇인가요?
- 좋은 답변의 신호: 인메모리 상태가 인스턴스마다 갈라지는 지점을 구체적으로 짚는다. / 바꿀 범위를 코드의 구체적인 위치로 말한다.
- 우려 신호: 인스턴스를 늘리면 된다는 말만 하고 상태 공유를 언급하지 않는다. / 멱등성 기록과 재고 중 무엇이 문제인지 구분하지 못한다.
- 근거: 기준 R-01, 기준 R-05, 기준 R-06

#### Q4. G1 그룹에서 재고 부족 검사와 수량 하한을 바꾼 변형이 살아남았습니다. 이 그룹의 테스트를 설계할 때 어떤 입력을 먼저 고르셨나요? <sub>테스트 설계 · SHOULD · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G1`
- 의도: 경계·검증 테스트에서 입력을 고르는 기준이 있는지 확인한다.
- 꼬리 질문: 수량 0을 받아들이는 변형은 어떤 테스트에서 걸렸어야 한다고 보시나요? / 재고보다 많은 수량을 고를 때 어떤 기준을 쓰시나요? / 단언을 더 촘촘하게 만든다면 무엇을 먼저 바꾸시겠어요?
- 좋은 답변의 신호: 살아남은 변형을 잡을 입력을 구체적인 값으로 말한다. / 테스트가 확인하는 동작과 구현 세부를 구분해 설명한다.
- 우려 신호: 테스트를 더 쓰겠다고만 말하고 대상을 정하지 않는다. / 변형이 살아남은 이유를 도구 문제로 돌린다.
- 근거: 기준 G1, 변이 M-01, 변이 M-02, 코드 src/index.ts:202-202, 코드 src/index.ts:157-157

#### Q5. src/index.ts:32-34에 플래그 조건을 반복하며 기다리는 코드가 있습니다. 이 반복이 필요한 상황을 어떻게 설명하시겠어요? <sub>설계 트레이드오프 · SHOULD · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:busy-waits`
- 의도: 대기 반복을 둔 이유와 그 영향 범위를 스스로 설명하는지 확인한다.
- 꼬리 질문: 이 반복이 끝나는 조건은 무엇인가요? / 같은 요청이 겹칠 때 이 대기가 어떤 영향을 주나요? / 대기 방식을 바꾼다면 무엇을 먼저 고치시겠어요?
- 좋은 답변의 신호: 대기가 막으려는 경합 상황을 구체적으로 설명한다. / 대기 중 다른 요청의 처리에 미치는 영향을 언급한다.
- 우려 신호: 대기가 왜 있는지 설명하지 못한다. / 반복 조건이 끝나지 않는 경우를 생각하지 않는다.
- 근거: 코드 src/index.ts:32-34, 기준 R-12

#### Q6. 프로세스를 다시 시작해도 주문과 상태가 남아야 하는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요? <sub>요구사항 확장 · SHOULD · 6분 · TRADEOFFS · LLM</sub>

- 슬롯: `EXTENSION:persistent-store`
- 의도: 저장 요구가 바뀔 때 구조를 어디부터 손대는지 확인한다.
- 꼬리 질문: 그 부분을 먼저 고르신 이유는 무엇인가요? / 저장 위치를 바꾸면 기존 동작 중 무엇을 다시 확인하시겠어요? / 초기화 경로는 새 저장 구조에서 어떻게 동작해야 하나요?
- 좋은 답변의 신호: 바꿀 범위를 코드의 구체적인 위치로 말한다. / 저장 위치를 옮길 때 새로 생기는 실패 상황을 든다.
- 우려 신호: 도구 이름만 대고 코드에서 바꿀 부분을 말하지 않는다. / 초기화 경로와의 관계를 언급하지 않는다.
- 근거: 기준 R-01, 기준 R-02, 기준 R-05

#### Q7. 클린 아키텍처와 TDD를 적용한 프로젝트에서 계층을 나눈 기준은 무엇이었나요? <sub>이력서 연결 · SHOULD · 5분 · DESIGN · LLM</sub>

- 슬롯: `RESUME_BRIDGE:1`
- 의도: 이력서에 적힌 클린 아키텍처·TDD 적용 경험의 실제 범위와 판단 기준을 확인한다.
- 꼬리 질문: 테스트를 먼저 작성한 범위는 어디까지였나요? / 경계가 되는 검증 규칙은 어느 계층에 두었나요? / 이번 과제처럼 테스트가 결함을 놓치는 상황을 어떻게 발견하고 보완하시나요?
- 좋은 답변의 신호: 계층별 책임과 의존 방향을 자기 프로젝트 예로 구체적으로 설명한다 / 테스트가 검증하는 대상과 검증하지 못하는 대상을 구분해 말한다
- 우려 신호: 클린 아키텍처를 폴더 구조 이야기로만 설명한다 / 테스트를 먼저 작성한 범위를 구체적으로 제시하지 못한다
- 근거: 맥락 연결 c15a3fac, 기준 G1

#### Q8. G2 그룹에서 멱등 키 조회와 본문 비교를 건너뛰는 변형이 살아남았습니다. 이 그룹의 테스트를 설계할 때 어떤 호출 순서를 먼저 정하셨나요? <sub>테스트 설계 · OPTIONAL · 6분 · TESTING · LLM</sub>

- 슬롯: `TEST_DESIGN:G2`
- 의도: 멱등성 테스트에서 재전송과 충돌을 구분하는 설계 기준이 있는지 확인한다.
- 꼬리 질문: 같은 키로 다른 본문을 보내는 경우를 어떤 테스트로 확인하시겠어요? / 재전송 후 재고가 한 번만 줄었는지 어떻게 확인하시겠어요? / 두 호출의 응답 id를 비교하는 단언을 어디에 두셨나요?
- 좋은 답변의 신호: 재전송과 충돌을 서로 다른 입력으로 나눠 설계한다. / 재고 변화량까지 단언에 포함한다.
- 우려 신호: 같은 키 재전송만 확인하고 본문이 다른 경우를 다루지 않는다. / 응답 상태만 보고 id나 재고를 확인하지 않는다.
- 근거: 기준 G2, 변이 M-03, 변이 M-04, 코드 src/index.ts:170-170, 코드 src/index.ts:177-177

#### Q9. src/index.ts:101-233의 주문 생성 처리가 133줄로 가장 깁니다. 이 안에서 요청 파싱과 도메인 규칙을 어떻게 나누셨나요? <sub>설계 트레이드오프 · OPTIONAL · 6분 · DESIGN · LLM</sub>

- 슬롯: `DESIGN_TRADEOFF:max-function`
- 의도: 한 함수 안에서 책임을 나눈 기준이 있는지 확인한다.
- 꼬리 질문: 재고 차감과 멱등성 기록 중 어느 쪽이 먼저 실행되나요? / 이 함수에서 HTTP 요청·응답 객체에 의존하는 부분은 어디까지인가요? / 책임을 나눈다면 어느 경계를 먼저 그으시겠어요?
- 좋은 답변의 신호: 파싱과 도메인 규칙의 경계를 코드 위치로 구분해 설명한다. / 지금 구조에서 생기는 구체적인 불편을 예로 든다.
- 우려 신호: 함수가 길다는 사실만 인정하고 나눌 기준을 말하지 않는다. / 도메인 규칙이 요청 객체에 의존하는지 여부를 구분하지 못한다.
- 근거: 코드 src/index.ts:101-233, 기준 R-12

#### Q10. 온라인몰 주문 API에서 성능 개선은 어떤 지표를 기준으로 진행하셨나요? <sub>이력서 연결 · OPTIONAL · 5분 · TRADEOFFS · LLM</sub>

- 슬롯: `RESUME_BRIDGE:2`
- 의도: 성능 개선 경험에서 측정 지표와 개선 판단 근거를 확인한다.
- 꼬리 질문: 개선 전후 수치는 어떤 도구로 측정했나요? / Redis는 어떤 데이터를 담당했나요? / 캐시와 DB 정합성은 어떻게 맞추셨나요?
- 좋은 답변의 신호: 측정 도구와 측정 조건을 구체적으로 설명한다 / 캐시 적용 범위와 무효화 시점을 설명한다
- 우려 신호: 개선 수치를 측정 방법 없이 제시한다 / 캐시 정합성 문제를 다뤄본 적 없다고 답한다
- 근거: 맥락 연결 4cb888c2, 기준 R-05

#### Q11. CSV 파싱을 외부 라이브러리 없이 직접 구현하신 이유는 무엇인가요? <sub>이력서 연결 · OPTIONAL · 5분 · DATA_INTEGRITY · LLM</sub>

- 슬롯: `RESUME_BRIDGE:3`
- 의도: 직접 구현 선택의 근거와 입력 형식 가정의 한계 인식을 확인한다.
- 꼬리 질문: 따옴표로 감싼 필드나 값 안의 콤마는 어떻게 처리하나요? / 입력 파일이 커지면 파싱 방식이 어떻게 달라져야 하나요? / 집계 결과가 잘못 나왔을 때 원인을 어떻게 좁히시나요?
- 좋은 답변의 신호: 지원하지 않는 입력 형식과 그 이유를 명확히 설명한다 / 파싱 실패 시 검증 지점을 제시한다
- 우려 신호: 모든 CSV를 처리한다고 일반화해 답한다 / 입력 형식 가정의 한계를 인식하지 못한다
- 근거: 맥락 연결 a8aa72f0

## 면접관 검토표 (사람 확인)

기술 면접관이 필수 질문을 읽고 "그대로 면접에 쓸 수 있는가"를 질문별로 표시한다. 고칠 점이 있으면 메모에 적는다.

| 페르소나 | 질문                                                                                                                                                                                | 유형          | 그대로 쓸 수 있는가 (예/아니오) | 메모 |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------- | ---- |
| seojin   | Q1. R-03 재고 부족 거절이 기대대로 동작했습니다. 재고와 요청 수량을 비교하는 판단을 코드의 어느 지점에 두셨나요?                                                                    | 강점 확인     |                                 |      |
| seojin   | Q2. R-04 입력 검증이 기대대로 동작했습니다. quantity와 productId를 검사하는 규칙을 어디에 모아 두셨나요?                                                                            | 강점 확인     |                                 |      |
| seojin   | Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어오는 상황을 가정하겠습니다. 지금 구조에서 가장 먼저 문제가 드러날 지점은 어디인가요?                         | 요구사항 확장 |                                 |      |
| taeyun   | Q1. R-06 실행 기록에서 같은 키로 다른 본문을 보냈을 때 201이 돌아왔습니다. 요청이 들어온 뒤 저장된 기록과 본문을 비교하는 지점을 코드 어디에 두셨나요?                              | 실패 디브리핑 |                                 |      |
| taeyun   | Q2. R-07 실행 기록에서 같은 키의 동시 요청 10건 중 5건만 201이고 응답 id가 서로 달랐습니다. 같은 키의 요청이 동시에 들어올 때 주문 생성이 어떤 순서로 진행되는지 설명해 주시겠어요? | 실패 디브리핑 |                                 |      |
| taeyun   | Q3. R-03은 재고 부족 요청을 409로 거절하고 재고를 그대로 두었습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요?                                                            | 강점 확인     |                                 |      |
| gaeun    | Q1. R-05 재전송 케이스에서 두 번째 호출이 새 주문을 만들었고 재고도 두 번 줄었습니다. 같은 키의 두 번째 요청을 처리하는 흐름을 코드에서 어떻게 따라가 보시겠어요?                   | 실패 디브리핑 |                                 |      |
| gaeun    | Q2. R-06 충돌 케이스에서 다른 본문의 재요청이 201로 통과했습니다. 같은 키로 들어온 요청의 본문이 이전과 같은지 판단하는 부분을 어디에 두셨나요?                                     | 실패 디브리핑 |                                 |      |
| gaeun    | Q3. G1 그룹에서 재고 부족 검사를 없앤 변형이 제출 테스트를 통과했습니다. 재고 부족 상황을 검증하는 테스트를 설계할 때 어떤 입력을 고르셨나요?                                       | 테스트 설계   |                                 |      |
| gaeun    | Q4. R-03은 재고 부족 요청을 409로 거절하고 재고를 그대로 두었습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요?                                                            | 강점 확인     |                                 |      |
| dohyun   | Q1. R-03은 재고보다 많은 수량과 재고 0인 상품 요청을 모두 거절했습니다. 재고와 요청 수량을 비교하는 판단을 어디에 두셨나요?                                                         | 강점 확인     |                                 |      |
| dohyun   | Q2. R-04는 여러 잘못된 입력을 400으로 거절했습니다. 수량과 상품 식별자 검증을 어느 지점에서 하기로 정하셨나요?                                                                      | 강점 확인     |                                 |      |
| dohyun   | Q3. 같은 서비스를 여러 인스턴스로 띄워 요청이 서로 다른 인스턴스로 들어가는 상황을 가정해 보겠습니다. 지금 구조에서 가장 먼저 바꾸실 부분은 어디인가요?                             | 요구사항 확장 |                                 |      |
